# Requirements: Prioritas v1.3 — E2E Realtime Hardening

**Status:** Filed (not started)
**Opened:** 2026-04-11
**Origin:** 6-pipeline E2E triage arc (2026-04-10 → 2026-04-11) closed after 3+ consecutive pipelines with unchanged pass count. Remaining failures are a different error class (React realtime rendering + invite-flow navigation + pre-existing RPC bug) that is mismatched with per-test Eva orchestration against CI. This milestone re-scopes the remaining work with a local reproduction environment as prerequisite.

**Scope principle:** E2E and integration test stabilization only. Fix the CI Integration Tests workflow from **3/10 green → 10/10 green**. No new features. No refactors beyond what a failing test forces.

## Baseline — CI state at filing (commit `568a72e`, run 24286409806)

**Integration Tests (Supabase) workflow:** `failure`

| ID | Status | Failure mode |
|----|--------|--------------|
| T-055-100 | ✅ pass | owner sends invitation |
| T-055-101 | ❌ fail | invitee accepts — `page.waitForURL('project=<id>')` timeout at `invite-flow.spec.ts:228` |
| T-055-102 | ✅ pass | expired invite state |
| T-055-103 | ✅ pass | 401 without auth |
| T-054B-300 | ❌ fail | `two browsers see each other in presence stack` — `toHaveCount(2)` mismatch |
| T-054B-301 | ❌ fail | `cursor appears in browser B when A moves mouse` — `getAttribute` timeout |
| T-054B-302 | ❌ fail | `drag starts lock overlay in browser B` — element not visible |
| T-054B-303 | ❌ fail | `drop propagates position in browser B within 2s` |
| T-054B-304 | ❌ fail | `disconnect shows reconnecting badge (Playwright route block)` |
| T-054B-305 | ❌ fail | `reconnect shows recovery toast` |

**Vitest migration tests:** 6 passing / 1 failing
- `phase05.3-migrations.integration.test.ts:233` — `accept_invitation` RPC returns `{ code: 'P0001', message: 'invalid_or_expired' }`. Pre-existing separate bug.

## Key CI log signal (from previous arc, worth acting on first)

```
strict mode violation: locator('[data-testid="design-matrix"]') resolved to 2 elements
```

Strong signal that the matrix is rendering **twice** after our URL-routing fix from pipeline 1. Likely `MatrixFullScreenView.tsx` + `DesignMatrix.tsx` lifecycle overlap. One render-lifecycle fix could unblock multiple T-054B tests simultaneously. This is where v1.3 should start after the prereq phase lands.

## Requirements

### Prerequisite — Debug environment

- **E2E-00**: A local CI-equivalent reproduction environment exists. Developer can run `supabase start`, seed GoTrue users with Admin API, run the failing Playwright specs against `localhost:3003`, and iterate in seconds rather than 5-pipeline CI cycles. Documented runbook in `.planning/phases/11-local-ci-repro/`. No more CI-log-based debugging of realtime React code.

### Realtime rendering (React/UI layer) — 6 tests

- **E2E-01**: T-054B-300 (presence stack — `toHaveCount(2)`) passes in CI
- **E2E-02**: T-054B-301 (cursor broadcast — `getAttribute` timeout) passes in CI
- **E2E-03**: T-054B-302 (drag starts lock overlay in browser B) passes in CI
- **E2E-04**: T-054B-303 (drop propagates position within 2s) passes in CI
- **E2E-05**: T-054B-304 (reconnecting badge on disconnect) passes in CI
- **E2E-06**: T-054B-305 (recovery toast on reconnect) passes in CI
- **E2E-07**: No `strict mode violation: 2 design-matrix elements` in any Playwright log — the matrix renders exactly once per route state

### Invite flow — 1 test

- **E2E-08**: T-055-101 (invitee accepts invitation — post-accept navigation to `/?project=<id>`) passes in CI. Fix scope: `src/pages/InvitationAcceptPage.tsx` redirect logic after `POST /api/invitations/accept` success.

### Migration RPC — 1 test

- **E2E-09**: `phase05.3-migrations.integration.test.ts:233` passes. Fix scope: the `accept_invitation` RPC definition in the Phase 05.2 migration (`supabase/migrations/20260408140000_phase5_accept_invitation_digest_fix.sql`). Currently returns `P0001 invalid_or_expired` where the test expects success. Likely token-hashing or expiry comparison bug.

### Pattern hygiene carried over from v1.2 arc

- **TECH-01**: `src/lib/services/CollaborationService.ts:97` fixed — same `project_collaborators.select('id')` → `.select('project_id')` pattern as commit `568a72e`. Roz flagged during the v1.2 wave QA but fix was not shipped in the arc.
- **TECH-02**: Follow-up ADR on silent error swallowing in `validateProjectAccess` collab/admin queries. Re-introduce `error:` destructuring and `console.error` for non-`PGRST116` codes. Flagged by Poirot blind review during arc. ADR + implementation.

## Success Criteria

1. **CI Integration Tests workflow: 10/10 green** on a commit tagged `v1.3`.
2. **Vitest migration tests: 7/7 green** on the same commit.
3. **Local repro runbook documented and validated** — a second developer (or the filer after a week) can set up the local env and reproduce a T-054B failure from the baseline without asking questions.
4. **Pattern-hygiene items shipped in the same milestone** — TECH-01 + TECH-02 must not slip to v1.4.

## Out of Scope (explicitly)

- New features of any kind
- Performance optimization beyond what a failing test forces
- Refactoring `ScopedRealtimeManager` or `BrainstormRealtimeManager` beyond fixing specific test failures
- Backfilling the 5 `test.todo` DndKit sensor tests deferred from v1.1 (separate concern)
- v1.2 Phase 10 follow-ups beyond TECH-01/TECH-02 above

## Deferred Decisions

- Whether TECH-02 (silent error swallowing ADR) is blocking for v1.3 close or ships as a follow-up — revisit after the phase plan is drafted
- Whether to tag v1.3 or simply close out and resume main-branch work — depends on whether any production-affecting fixes land alongside the test fixes

---

**Related artifacts:**
- Previous arc state: `docs/pipeline/pipeline-state.md` (session 2026-04-11)
- Arc commits: `a10357a`, `336c8d8`, `acaf089`, `8b19c41`, `568a72e`
- Retro lessons that drove filing this milestone rather than continuing the arc: "onion-peel strategy change signal" (3+ pipelines / unchanged pass count → change strategy)
