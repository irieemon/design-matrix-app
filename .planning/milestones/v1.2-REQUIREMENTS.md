# Requirements: Prioritas v1.2 (ARCHIVED)

**Shipped:** 2026-04-11
**Status:** 7/7 complete (100%)
**Milestone:** v1.2 — Production Hardening

## Scope Principle

**No new features.** v1.2 was strictly ops, infra, and bug fixes. New features were deferred to v1.3+.

## v1.2 Requirements — Final Status

### Operations (carried from v1.0/v1.1) — 3/3 ✅

- [x] **OPS-01**: Resend domain verification — production email delivery from a verified sender domain. Closed via config runbook in Phase 08 (zero code changes — operational setup only).
- [x] **OPS-02**: Fix analyze-file 403 CSRF race — `waitForCsrfToken` polling utility (~50 LOC) added in Phase 08, wired into `FileService`. 9 tests added.
- [x] **OPS-03**: AI Gateway project-wide migration — `transcribeAudio.ts` summary step migrated through Vercel AI Gateway in Phase 08 (last raw fetch eliminated, 2-line fix).

### Bug Fixes — 1/1 ✅

- [x] **BUG-01**: Fix `RealtimeSubscriptionManager.subscribeToIdeas` clearing bug — event-merge pattern (Option B) replaces `callback([])` in Phase 09. D-34 workaround removed from `useProjectRealtime`. BRM 43/43 tests unchanged. ADR-0009 documents root cause + fix.

### CI / Test Infrastructure — 3/3 ✅

- [x] **CI-01**: Enable `test.skip` E2E tests in CI — `invite-flow.spec.ts` and `project-realtime-matrix.spec.ts` now run in CI via `CI_SUPABASE` conditional guards (Phase 10-01).
- [x] **CI-02**: Enable `test.skip` integration tests in CI — `phase05.3-migrations.integration.test.ts` runs in CI with the same conditional pattern (Phase 10-01).
- [x] **CI-03**: Seed data strategy — `supabase/seed.sql` (deterministic 3-user + 1-project + 2-invitation fixture) + GoTrue Admin API user seeding via workflow inline step (Phase 10-01 + 10-02).

## Traceability — Final

| Requirement | Phase | Status | Note |
|-------------|-------|--------|------|
| OPS-01 | Phase 08 | ✅ Complete | Config runbook only (no code) |
| OPS-02 | Phase 08 | ✅ Complete | `waitForCsrfToken` + FileService wiring |
| OPS-03 | Phase 08 | ✅ Complete | `transcribeAudio` 2-line fix |
| BUG-01 | Phase 09 | ✅ Complete | Event-merge pattern, ADR-0009 |
| CI-01 | Phase 10 | ✅ Complete | CI_SUPABASE conditionals + workflow |
| CI-02 | Phase 10 | ✅ Complete | Vitest conditional early-return guard |
| CI-03 | Phase 10 | ✅ Complete | `seed.sql` + GoTrue Admin API seeding |

## Known Gaps Carried to v1.3

All 7 v1.2 requirements shipped, but the newly-enabled CI surfaced a class of failures that did not exist in the v1.2 scope. CI Integration Tests closed at **3/10 passing** after a 6-pipeline E2E triage arc (access-control layer fully resolved; remaining failures are React realtime / invite-flow / pre-existing RPC bug). These are re-scoped into v1.3.

| Carried item | Destination | Reason |
|--------------|-------------|--------|
| T-054B-300..305 (6 tests — realtime UI) | v1.3 / Phase 12 | React render lifecycle (not access-control) |
| T-055-101 (post-accept navigation) | v1.3 / Phase 13 | Different failure mode than arc-fixed items |
| `phase05.3-migrations` `accept_invitation` RPC P0001 | v1.3 / Phase 13 | Pre-existing separate bug (not caused by v1.2 work) |
| `CollaborationService.ts:97` pattern fix | v1.3 / TECH-01 | Roz flagged during arc; fix not shipped |
| Silent error swallowing in `validateProjectAccess` | v1.3 / TECH-02 | Poirot flagged; follow-up ADR needed |
| Local CI reproduction environment | v1.3 / Phase 11 | Prerequisite — not in v1.2 scope |

See `.planning/milestones/v1.3-REQUIREMENTS.md` for the v1.3 re-scope.

---
*Archived 2026-04-11 during `/gsd-complete-milestone 1.2`. Manually rewritten by Eva — the gsd-tools CLI produced an archive with requirements still unchecked.*
