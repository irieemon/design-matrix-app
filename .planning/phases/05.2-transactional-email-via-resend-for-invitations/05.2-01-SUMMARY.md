---
phase: 05.2-transactional-email-via-resend-for-invitations
plan: 01
subsystem: transactional-email
tags: [email, resend, invitations, template]
requires: []
provides:
  - "buildInviteEmail() pure template builder"
  - "resend SDK installed"
  - "RESEND_API_KEY / RESEND_FROM_EMAIL documented"
affects: [api/_lib]
tech_added: [resend@^6.10.0]
patterns: [pure-function-template, html-escape-local]
files_created:
  - api/_lib/inviteEmailTemplate.ts
  - api/_lib/__tests__/inviteEmailTemplate.test.ts
files_modified:
  - package.json
  - package-lock.json
  - .env.example
decisions:
  - "Inline HTML template, no React Email / MJML (D-08)"
  - "Local HTML escape helper instead of new dep"
  - "Pure function — no env reads, no I/O — so Plan 02 owns all wiring"
metrics:
  tasks_completed: 2
  duration_minutes: ~5
  completed_date: 2026-04-07
---

# Phase 05.2 Plan 01: Resend SDK + Invite Email Template — Summary

Installed the Resend Node SDK, documented the two new backend env vars, and shipped a pure `buildInviteEmail` template module (HTML + plain-text) consumed by Plan 02's wiring into `POST /api/invitations/create`.

## What was built

- **Task 1** — `npm install resend` (^6.10.0). Added `RESEND_API_KEY` and `RESEND_FROM_EMAIL=onboarding@resend.dev` to `.env.example` with a comment explaining backend-only usage and the onboarding-sender default. Commit `bf197ea`.
- **Task 2 (TDD)** — `api/_lib/inviteEmailTemplate.ts` exports `buildInviteEmail({projectName, inviterName, inviterEmail, role, inviteUrl}) → {subject, html, text}`. Sapphire-500 CTA button, escaped project/inviter fields, plain-text fallback paragraph with the URL, "ignore if unexpected" footer. Subject is exactly `"You've been invited to {projectName} on Prioritas"` per D-10. Co-located vitest suite (6 cases) covers subject, href embedding + role, text alternative, inviter-name fallback to email, footer presence, and HTML escaping. Commit `f1740ce`.

## Verification

- `npx vitest run api/_lib/__tests__/inviteEmailTemplate.test.ts` — 6/6 passing.
- Module imports nothing stateful and no `resend` symbol; Plan 02 will own SDK instantiation.
- `grep "You've been invited to" api/_lib/inviteEmailTemplate.ts` — present.

## Deviations from Plan

None. Plan executed as written.

## Deferred Issues

- `npm run type-check` reports ~15 pre-existing `TS2304` errors in `src/utils/{networkPerformanceMonitor,performanceTestRunner,performanceUtils,realtimeDiagnostic,roadmapExport}.ts` (unscoped catch identifiers). Out of scope for this plan — none of the new files in `api/_lib/` are flagged. Logged here for a future hardening pass.

## Follow-ups for Plan 02

- Import `buildInviteEmail` from `api/_lib/inviteEmailTemplate` in `api/invitations/create.ts`.
- Instantiate `new Resend(process.env.RESEND_API_KEY)` lazily; if the key is absent, log a warning and `console.log` the email body (D-04 fallback).
- Send AFTER the DB insert; never block the JSON response on email failure (D-12, D-13).
- Use `RESEND_FROM_EMAIL` with `onboarding@resend.dev` default.

## Self-Check: PASSED

- FOUND: api/_lib/inviteEmailTemplate.ts
- FOUND: api/_lib/__tests__/inviteEmailTemplate.test.ts
- FOUND commit: bf197ea
- FOUND commit: f1740ce
