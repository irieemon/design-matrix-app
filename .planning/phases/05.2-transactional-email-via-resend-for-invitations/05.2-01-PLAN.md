---
phase: 05.2-transactional-email-via-resend-for-invitations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - api/_lib/inviteEmailTemplate.ts
autonomous: true
requirements: [COLLAB-04]
must_haves:
  truths:
    - "Resend SDK is installed and importable"
    - "Inline HTML+text invite email template module exists and is pure (no side effects)"
    - "RESEND_API_KEY and RESEND_FROM_EMAIL are documented in .env.example"
  artifacts:
    - path: api/_lib/inviteEmailTemplate.ts
      provides: "buildInviteEmail({projectName, inviterName, role, inviteUrl}) → {subject, html, text}"
    - path: package.json
      contains: '"resend"'
    - path: .env.example
      contains: "RESEND_API_KEY"
  key_links:
    - from: api/_lib/inviteEmailTemplate.ts
      to: "Plan 02 (api/invitations/create.ts)"
      via: "named export buildInviteEmail"
---

<objective>
Install Resend SDK, document env vars, and create the pure email template module that Plan 02 will consume.

Purpose: Isolate template + dependency setup so Plan 02 can focus on wiring.
Output: resend dependency, .env.example update, api/_lib/inviteEmailTemplate.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-transactional-email-via-resend-for-invitations/05.2-CONTEXT.md
@api/_lib/invitationTokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install resend SDK and document env vars</name>
  <files>package.json, package-lock.json, .env.example</files>
  <read_first>package.json, .env.example</read_first>
  <action>
    Run `npm install resend` in design-matrix-app/ to add the Resend Node SDK (per D-01).
    Append to .env.example (per D-02, D-06):
      RESEND_API_KEY=
      RESEND_FROM_EMAIL=onboarding@resend.dev
    Add a brief comment above them: "# Transactional email (invitations). Backend only. Default sender is Resend onboarding inbox until a verified domain is configured."
  </action>
  <verify>
    <automated>grep -q '"resend"' package.json &amp;&amp; grep -q 'RESEND_API_KEY' .env.example &amp;&amp; grep -q 'RESEND_FROM_EMAIL' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `resend` appears in package.json dependencies
    - package-lock.json updated
    - .env.example contains both RESEND_API_KEY and RESEND_FROM_EMAIL
  </acceptance_criteria>
  <done>resend installed; env vars documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create inviteEmailTemplate module</name>
  <files>api/_lib/inviteEmailTemplate.ts, api/_lib/__tests__/inviteEmailTemplate.test.ts</files>
  <read_first>api/_lib/invitationTokens.ts, .planning/phases/05.2-transactional-email-via-resend-for-invitations/05.2-CONTEXT.md</read_first>
  <behavior>
    - Test 1: buildInviteEmail returns subject exactly "You've been invited to Acme on Prioritas" when projectName="Acme" (D-10)
    - Test 2: html contains the inviteUrl as an href and the role label
    - Test 3: text alternative contains the inviteUrl as plain text and the project name
    - Test 4: When inviterName is undefined/empty, falls back to inviterEmail in copy
    - Test 5: html contains the project name and "ignore" footer phrase
  </behavior>
  <action>
    Create `api/_lib/inviteEmailTemplate.ts` exporting:
      ```ts
      export interface InviteEmailInput {
        projectName: string
        inviterName?: string | null
        inviterEmail: string
        role: 'viewer' | 'editor'
        inviteUrl: string
      }
      export interface InviteEmail { subject: string; html: string; text: string }
      export function buildInviteEmail(input: InviteEmailInput): InviteEmail
      ```
    Per D-08/D-09/D-10/D-11:
    - subject: `You've been invited to ${projectName} on Prioritas`
    - html: hand-written inline-styled string. Sapphire-500 (#0ea5e9-ish) CTA button linking to inviteUrl, project name + role + inviter shown, plain-text URL paragraph below the button for clients that strip buttons, footer "If you didn't expect this invitation, you can safely ignore this email."
    - text: plain-text version with same info and the bare URL on its own line.
    - Escape projectName / inviterName for HTML (simple replace of &amp;&lt;&gt;&quot;&apos;) — do NOT pull in a new dep.
    - Pure function, no env reads, no I/O.
    Write the test file co-located under `api/_lib/__tests__/inviteEmailTemplate.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>npx vitest run api/_lib/__tests__/inviteEmailTemplate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `api/_lib/inviteEmailTemplate.ts` exists and exports `buildInviteEmail`
    - `grep -q "You've been invited to" api/_lib/inviteEmailTemplate.ts`
    - All tests in inviteEmailTemplate.test.ts pass
    - No imports from `resend` or anything stateful in the template module
  </acceptance_criteria>
  <done>Pure template module exists with passing tests.</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npx vitest run api/_lib/__tests__/inviteEmailTemplate.test.ts` passes
</verification>

<success_criteria>
Resend installed, env vars documented, pure template module ready for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/05.2-transactional-email-via-resend-for-invitations/05.2-01-SUMMARY.md`
</output>
