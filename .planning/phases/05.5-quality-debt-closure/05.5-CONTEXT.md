---
phase: 05.5-quality-debt-closure
milestone: v1.1
opened: 2026-04-10T01:15:00Z
orchestration: eva-pipeline + gsd-mirror
status: planning
requirements: [MOB-02-verify, QA-01, QA-02, QA-03]
depends_on: [05.4a, 05.4b]
sizing: Medium
---

# Phase 05.5: Quality Debt Closure — Context

## Goal

Close the quality debt carried forward from v1.0 and accumulated during v1.1 Phases 05.4a/05.4b so v1.1 ships with a clean regression baseline. No new features — test fixes, spec parity, verification checkpoints only.

## Scope

### QA-01: Test suite parity
**(a) Playwright mobile critical-paths spec** — 3 currently failing tests:
- `projects list submit button meets 44px touch target` — selector drift from Phase 07 DesktopOnlyHint refactor
- `MobileJoinPage loads and form inputs use 16px+ font` — selector drift
- `desktop-only page shows DesktopOnlyHint on mobile` — DesktopOnlyHint role changed from `status` → `note`, Tailwind → inline styles

**(b) BrainstormRealtimeManager.test.ts** — 22 pre-existing failures:
- All 22 are `ReferenceError: mockChannel is not defined` — mockChannel variable is referenced outside the scope where it was created by `getMockChannel()` helper
- Fix: restructure the mock setup so mockChannel is accessible to all tests in the describe block

### QA-02: Automated Playwright E2E for invite flow
- New test file: owner → invite → accept → collaborator-sees-project
- Currently manual-only (Phase 05.3 7-step verification)
- Requires dev server + Supabase; `test.skip` if not available (same pattern as 05.4b E2E)

### QA-03: SQL migration coverage
- pgTAP is NOT set up in this project (no `supabase/tests/` directory)
- Bootstrapping pgTAP framework is out of scope for a quality debt phase
- **Alternative:** Write Supabase integration tests via vitest that exercise the Phase 05.3 SQL functions (accept_invitation RPC, is_project_collaborator helper, projects SELECT RLS for collaborators) through the supabase-js client
- If integration tests aren't feasible (require live Supabase), document as `test.skip` with contract assertions

### MOB-02-verify: iOS Safari real-device verification
- **Human checkpoint** — cannot be automated
- Phase 04-02 Task 3: verify MIME fallback path, mic permission prompt, status-bar indicator, Files-app upload on real iOS Safari
- User must test on a physical iPhone or BrowserStack

### Accumulated tech debt (opportunistic, from 05.4a/05.4b)
Cherry-pick only items that are in test files being touched anyway:
- 05.4a Wave 3 T-054B-260/262/267/268/273 already `test.todo` — leave as-is
- 05.4b Wave 2 Finding 5 `prefersReducedMotion` synchronous read — defer to v1.2
- 05.4a T-054A-002 double-invocation, T-054A-155 redundancy — defer
- BUG-01 (RealtimeSubscriptionManager.subscribeToIdeas clearing ideas) — separate bug, defer to v1.2

## Pipeline sequence (abbreviated — Medium sizing, no Cal/Sable needed)

1. Eva seeds CONTEXT (done)
2. MOB-02 human checkpoint — ask user
3. Colby implements QA-01(a), QA-01(b), QA-02, QA-03
4. Roz verifies all 4 items pass
5. Commit + push
6. Phase VERIFICATION + SUMMARY closure

## Budget target

Medium: ~200-400k tokens total (test fixes only, no architecture)
