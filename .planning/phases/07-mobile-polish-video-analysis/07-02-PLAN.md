---
phase: 07-mobile-polish-video-analysis
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/auth/AuthScreen.tsx
  - src/components/pages/ProjectsPage.tsx
  - src/components/matrix/MatrixFullScreenView.tsx
  - src/components/brainstorm/MobileJoinForm.tsx
  - src/pages/MobileJoinPage.tsx
  - src/components/pages/SettingsPage.tsx
  - src/components/shared/DesktopOnlyHint.tsx
  - src/components/shared/BottomSheet.tsx
  - tests/e2e/mobile-critical-paths.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [MOB-03, MOB-04, MOB-05]
must_haves:
  truths:
    - "All D-09 critical paths render without horizontal scroll at 375px viewport"
    - "All interactive elements on critical paths meet 44x44px touch target (min-h-11 / min-w-11)"
    - "Form inputs on critical paths use text-base (16px+) so iOS Safari does not zoom on focus"
    - "Mobile-only users navigating to a desktop-only page see DesktopOnlyHint banner"
    - "QR join flow (MobileJoinPage → MobileJoinForm) works on iPhone 14 Pro and Galaxy S21 Playwright viewports"
    - "Idea submission on brainstorm uses BottomSheet on mobile viewports"
  artifacts:
    - path: "src/components/shared/BottomSheet.tsx"
      provides: "BottomSheet modal primitive for mobile"
    - path: "src/components/shared/DesktopOnlyHint.tsx"
      provides: "Best on desktop hint banner"
    - path: "tests/e2e/mobile-critical-paths.spec.ts"
      provides: "E2E coverage of critical paths at mobile viewports"
  key_links:
    - from: "src/components/layout/PageRouter.tsx (or equivalent)"
      to: "DesktopOnlyHint"
      via: "useBreakpoint + page classification from 07-MOBILE-AUDIT.md"
      pattern: "DesktopOnlyHint"
    - from: "src/pages/MobileJoinPage.tsx form inputs"
      to: "iOS Safari zoom prevention"
      via: "text-base class on inputs"
      pattern: "text-base|text-\\[16px\\]"
---

<objective>
Track A critical-path mobile polish. Consumes the 07-MOBILE-AUDIT.md work list from 07-01 and delivers responsive + touch polish on the D-09 critical paths: authentication, projects list, matrix view (companion mode), brainstorm session (including QR join), and settings. Ships two shared primitives (`BottomSheet`, `DesktopOnlyHint`) and a Playwright mobile critical-path suite.

Purpose: Make the companion experience actually usable on real phones. Addresses MOB-03 (responsive polish), MOB-04 (touch interactions + mobile enhancements), MOB-05 (QR join reliability).
Output: 5 critical-path pages polished, 2 shared primitives, E2E coverage at iPhone 14 Pro (393px) and Galaxy S21 (360px) viewports.

Addresses: MOB-03, MOB-04, MOB-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md
@.planning/phases/07-mobile-polish-video-analysis/07-CONTEXT.md
@.planning/phases/07-mobile-polish-video-analysis/07-RESEARCH.md
@src/hooks/useBreakpoint.ts
@src/pages/MobileJoinPage.tsx
@src/components/layout/PageRouter.tsx
@playwright.config.ts

<interfaces>
From src/hooks/useBreakpoint.ts (created in 07-01):
```ts
export interface Breakpoint { isMobile: boolean; isTablet: boolean; isDesktop: boolean; width: number }
export function useBreakpoint(): Breakpoint
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared primitives — BottomSheet + DesktopOnlyHint (D-07, D-10)</name>
  <files>src/components/shared/BottomSheet.tsx, src/components/shared/DesktopOnlyHint.tsx, src/components/layout/PageRouter.tsx</files>
  <read_first>
    - src/hooks/useBreakpoint.ts (from 07-01)
    - src/components/shared/Modal.tsx (to match existing modal conventions)
    - src/components/layout/PageRouter.tsx (to understand routing + where to gate DesktopOnlyHint)
    - 07-MOBILE-AUDIT.md (for the list of desktop-only routes)
  </read_first>
  <action>
    1. **`src/components/shared/BottomSheet.tsx`** — mobile-native bottom sheet modal.
       - Props: `{ isOpen: boolean; onClose: () => void; title?: string; children: ReactNode }`
       - Renders centered modal via existing Modal on desktop, slides up from bottom on mobile (branched via `useBreakpoint`).
       - Mobile styling: `fixed inset-x-0 bottom-0 rounded-t-2xl`, max-height `85vh`, overflow-y auto, swipe-down-to-close via touchstart/touchmove with a translateY threshold of 80px.
       - Backdrop click closes. Traps focus (re-use Modal's pattern).
       - Animation: tailwind `transition-transform duration-200` on mobile.

    2. **`src/components/shared/DesktopOnlyHint.tsx`** — "Best on desktop" banner (D-07).
       - Props: `{ pageName: string; emailLinkCta?: boolean }`
       - Renders a non-blocking banner at the top of the content area: "Best on desktop — [page] works better on a larger screen. [Email me this link]"
       - Email-me CTA opens `mailto:?subject=Prioritas link&body=<current url>`.
       - Only renders when `useBreakpoint().isMobile` is true. Returns `null` otherwise.

    3. **Wire DesktopOnlyHint into PageRouter.tsx**:
       - Import the audit's desktop-only route list (hardcode as a `const DESKTOP_ONLY_ROUTES` array sourced from 07-MOBILE-AUDIT.md).
       - In PageRouter, before rendering the routed page, check if the current route is in `DESKTOP_ONLY_ROUTES` AND `useBreakpoint().isMobile` — if so, render `<DesktopOnlyHint pageName={...} />` above the page content. Do NOT block rendering (D-08).
  </action>
  <verify>
    <automated>test -f src/components/shared/BottomSheet.tsx &amp;&amp; test -f src/components/shared/DesktopOnlyHint.tsx &amp;&amp; grep -q "DESKTOP_ONLY_ROUTES" src/components/layout/PageRouter.tsx &amp;&amp; grep -q "DesktopOnlyHint" src/components/layout/PageRouter.tsx &amp;&amp; npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - BottomSheet file exists with `isOpen`, `onClose`, `title`, `children` props
    - DesktopOnlyHint returns null when `!isMobile` (assert by code inspection + unit test if time permits)
    - PageRouter imports DesktopOnlyHint and has a `DESKTOP_ONLY_ROUTES` const with ≥3 routes from the audit
    - `npm run type-check` passes
  </acceptance_criteria>
  <done>Both primitives exist, PageRouter wired, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Critical-path responsive + touch polish (D-09, MOB-03, MOB-04, MOB-05)</name>
  <files>src/components/auth/AuthScreen.tsx, src/components/pages/ProjectsPage.tsx, src/components/matrix/MatrixFullScreenView.tsx, src/components/brainstorm/MobileJoinForm.tsx, src/pages/MobileJoinPage.tsx, src/components/pages/SettingsPage.tsx</files>
  <read_first>
    - Each file listed above (read fully before edit)
    - .claude/rules/frontend.md (touch target + responsive rules)
    - tailwind.config.js (confirm `min-h-11`, `min-w-11` = 44px available)
  </read_first>
  <action>
    For each of the 5 critical-path areas (auth, projects list, matrix view, brainstorm join/submit, settings):

    **Responsive fixes (MOB-03):**
    - No horizontal scroll at 375px viewport: fix any `min-w-[Npx]` where N > 343, replace fixed pixel widths on containers with fluid `w-full max-w-*` classes.
    - Stack multi-column layouts to single column below `md:` breakpoint (Tailwind convention).
    - Replace any `text-xs` in primary body copy on forms with `text-sm` or `text-base`.

    **Touch targets (MOB-04, WCAG 2.1 AA):**
    - Every `<button>`, link, and interactive element on the critical path gets `min-h-11 min-w-11` (44px) classes OR its content padding ensures ≥44px hit area.
    - Bump icon-only buttons from `p-2` to `p-3` minimum.

    **iOS zoom prevention (MOB-05):**
    - Every `<input>`, `<textarea>`, `<select>` on auth + MobileJoinForm + settings gets `text-base` class (16px) to prevent iOS Safari zoom-on-focus.

    **QR join specifics (MOB-05):**
    - `MobileJoinPage.tsx`: confirm no `getUserMedia` usage (research finding #3 — native camera scans the QR, we only receive the URL). If any `navigator.mediaDevices` call exists, delete it.
    - `MobileJoinForm.tsx`: all inputs `text-base`, submit button `min-h-11 w-full`, error messages `text-sm` red with sufficient contrast.
    - ITP risk for `generateDeviceFingerprint()`: if the fingerprint is persisted to localStorage on MobileJoinPage, wrap the read in try/catch and regenerate on failure. Add a comment citing research finding #3.

    **Bottom sheet swap (D-10):**
    - Wherever brainstorm idea submission currently opens a center-dialog modal, swap to `BottomSheet` from Task 1 when `useBreakpoint().isMobile` is true. (If the current component is a shared modal, add a prop `mobileVariant="bottom-sheet"`.)
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run lint -- src/components/auth/AuthScreen.tsx src/components/pages/ProjectsPage.tsx src/components/matrix/MatrixFullScreenView.tsx src/components/brainstorm/MobileJoinForm.tsx src/pages/MobileJoinPage.tsx src/components/pages/SettingsPage.tsx &amp;&amp; ! grep -rn "getUserMedia" src/pages/MobileJoinPage.tsx src/components/brainstorm/MobileJoinForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every edited file has at least one `min-h-11` or `min-w-11` added (grep each file)
    - MobileJoinForm and AuthScreen inputs have `text-base` class (grep)
    - `npm run type-check` passes
    - `npm run lint` passes on the edited files
    - `getUserMedia` is absent from MobileJoinPage and MobileJoinForm (research finding #3)
    - Brainstorm idea submission uses `BottomSheet` when `isMobile` (grep for `BottomSheet` import in the submission component)
  </acceptance_criteria>
  <done>All 5 critical paths touch-target-compliant, responsive at 360px+, iOS-zoom safe, QR join verified fingerprint-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright mobile critical-path E2E suite</name>
  <files>tests/e2e/mobile-critical-paths.spec.ts, playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (existing project definitions)
    - tests/e2e/ (existing spec patterns)
  </read_first>
  <action>
    1. **`playwright.config.ts`**: add (or confirm) two mobile projects using Playwright's `devices` presets:
       - `'Mobile Safari iPhone 14 Pro'` → `devices['iPhone 14 Pro']` (393x852)
       - `'Mobile Chrome Galaxy S21'` → `{ ...devices['Galaxy S9+'], viewport: { width: 360, height: 800 } }`

    2. **`tests/e2e/mobile-critical-paths.spec.ts`** with these tests (one `test()` per behavior):
       - `test('auth screen has no horizontal scroll at 375px')` — goto `/`, assert `document.documentElement.scrollWidth <= window.innerWidth`
       - `test('auth screen inputs are at least 44px tall')` — for each `input`, assert `boundingBox().height >= 44`
       - `test('projects list submit button meets 44px touch target')` — login demo user, visit /projects, assert primary button height ≥ 44
       - `test('MobileJoinPage loads and form inputs use 16px+ font')` — goto `/#join/demo-session`, assert computed `font-size` of first input ≥ 16px
       - `test('desktop-only page shows DesktopOnlyHint on mobile')` — goto a known desktop-only route (e.g. `/reports`), assert hint banner text "Best on desktop" is visible
       - `test('no horizontal scroll on matrix view at 360px')` — visit matrix page, assert scrollWidth constraint

    3. Every test uses `test.use({ ...devices['iPhone 14 Pro'] })` or runs under the mobile project.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/mobile-critical-paths.spec.ts --project="Mobile Safari iPhone 14 Pro" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Spec file contains 6 `test(` blocks matching the names above
    - `playwright.config.ts` has both mobile project entries
    - All 6 tests pass on at least the iPhone 14 Pro project (Galaxy S21 nice-to-have)
  </acceptance_criteria>
  <done>E2E suite green at mobile viewports covering the critical paths.</done>
</task>

</tasks>

<verification>
- `npm run type-check` clean
- `npm run lint` clean on edited files
- `npx playwright test tests/e2e/mobile-critical-paths.spec.ts` green
- Visual spot check on 393px viewport shows no horizontal scroll on D-09 critical paths
</verification>

<success_criteria>
D-09 critical paths (auth, projects list, matrix, brainstorm session incl. QR join, settings) are touch-compliant, responsive at 360px+, iOS-zoom-safe. BottomSheet + DesktopOnlyHint primitives shipped and wired. E2E suite enforces the invariants.
</success_criteria>

<output>
Create `.planning/phases/07-mobile-polish-video-analysis/07-02-SUMMARY.md` on completion.
</output>
