---
phase: 07-mobile-polish-video-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .env.example
  - vite.config.ts
  - src/hooks/useBreakpoint.ts
  - src/hooks/__tests__/useBreakpoint.test.ts
  - .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md
autonomous: true
requirements: [MOB-01]
must_haves:
  truths:
    - "All 4 VITE_MOBILE_BRAINSTORM_PHASE2..5 flags read `true` at runtime after dev restart"
    - "useBreakpoint hook returns is-mobile/is-tablet/is-desktop booleans based on window width"
    - "07-MOBILE-AUDIT.md exists and classifies every page as mobile-native|mobile-view-only|desktop-only"
  artifacts:
    - path: ".env.local"
      contains: "VITE_MOBILE_BRAINSTORM_PHASE2=true"
    - path: "vite.config.ts"
      contains: "VITE_MOBILE_BRAINSTORM_PHASE2"
    - path: "src/hooks/useBreakpoint.ts"
      provides: "useBreakpoint hook"
    - path: ".planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md"
      contains: "| Page | Classification |"
  key_links:
    - from: "src/lib/config.ts FEATURE_FLAGS"
      to: "import.meta.env.VITE_MOBILE_BRAINSTORM_PHASE*"
      via: "Vite static replacement"
      pattern: "VITE_MOBILE_BRAINSTORM_PHASE"
---

<objective>
Enable Track A mobile scaffolding: flip the 4 mobile brainstorm feature flags (D-03, D-04), ship the missing `useBreakpoint` hook that Track A polish depends on, and produce the authoritative `07-MOBILE-AUDIT.md` (D-01, D-02) that drives all downstream responsive work. This is the Wave 0 prerequisite for 07-02.

Purpose: Every mobile polish decision downstream depends on (a) the flags actually being on, (b) a shared viewport hook to branch on, and (c) a single source of truth classifying every page.
Output: Flags enabled in env + vite allowlist, `useBreakpoint` hook with unit tests, mobile audit document.

Addresses: MOB-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-mobile-polish-video-analysis/07-CONTEXT.md
@.planning/phases/07-mobile-polish-video-analysis/07-RESEARCH.md
@src/lib/config.ts
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable mobile brainstorm flags (D-03, D-04)</name>
  <files>.env.local, .env.example, vite.config.ts</files>
  <read_first>
    - src/lib/config.ts (lines 44-115) to confirm the 4 flag names
    - vite.config.ts to find the dev API env-var allowlist (Phase 05.2/06 pattern)
  </read_first>
  <action>
    1. Append to `.env.local`:
       ```
       VITE_MOBILE_BRAINSTORM_PHASE2=true
       VITE_MOBILE_BRAINSTORM_PHASE3=true
       VITE_MOBILE_BRAINSTORM_PHASE4=true
       VITE_MOBILE_BRAINSTORM_PHASE5=true
       ```
    2. Add the same 4 keys to `.env.example` with empty values and a comment "# Mobile brainstorm rollout flags (Phase 07)".
    3. In `vite.config.ts`, find the dev API env-var allowlist (the place Phase 05.2/06 added their env vars) and add all 4 `VITE_MOBILE_BRAINSTORM_PHASE*` keys.
    4. Restart the dev server (flags are `import.meta.env` static-replaced — HMR will NOT pick them up). Document this in the acceptance criteria below.
  </action>
  <verify>
    <automated>grep -q "VITE_MOBILE_BRAINSTORM_PHASE2=true" .env.local &amp;&amp; grep -q "VITE_MOBILE_BRAINSTORM_PHASE5=true" .env.local &amp;&amp; grep -q "VITE_MOBILE_BRAINSTORM_PHASE2" .env.example &amp;&amp; grep -q "VITE_MOBILE_BRAINSTORM_PHASE2" vite.config.ts &amp;&amp; grep -q "VITE_MOBILE_BRAINSTORM_PHASE5" vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` contains all 4 `VITE_MOBILE_BRAINSTORM_PHASE[2-5]=true` lines
    - `.env.example` contains all 4 keys
    - `vite.config.ts` allowlist contains all 4 keys (grep passes)
    - Executor restarts `npm run dev` after edits and pastes `console.log(FEATURE_FLAGS)` output into SUMMARY showing all 4 flags = true, OR adds a one-line temporary log in `src/main.tsx`, verifies, and removes
  </acceptance_criteria>
  <done>Flags enabled, allowlisted, and confirmed true at runtime after dev server restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useBreakpoint hook (research gap)</name>
  <files>src/hooks/useBreakpoint.ts, src/hooks/__tests__/useBreakpoint.test.ts</files>
  <read_first>
    - src/hooks/useAuth.ts (for project hook style/return-object conventions)
    - tailwind.config.js (to align breakpoints with Tailwind: sm=640, md=768, lg=1024)
  </read_first>
  <behavior>
    - Test 1: returns `{ isMobile: true, isTablet: false, isDesktop: false }` when window.innerWidth = 375
    - Test 2: returns `{ isMobile: false, isTablet: true, isDesktop: false }` when innerWidth = 768
    - Test 3: returns `{ isMobile: false, isTablet: false, isDesktop: true }` when innerWidth = 1280
    - Test 4: updates on window `resize` event (fire resize, assert new value)
    - Test 5: cleans up resize listener on unmount
  </behavior>
  <action>
    Create `src/hooks/useBreakpoint.ts` exporting:
    ```ts
    export interface Breakpoint { isMobile: boolean; isTablet: boolean; isDesktop: boolean; width: number }
    export function useBreakpoint(): Breakpoint
    ```
    Implementation:
    - Thresholds: mobile `< 768`, tablet `>= 768 && < 1024`, desktop `>= 1024` (matches Tailwind md/lg).
    - SSR-safe: default to desktop when `typeof window === 'undefined'`.
    - useState + useEffect with `window.addEventListener('resize', ...)` + cleanup.
    - Throttle not required for v1; resize handler sets state directly.
    Create Vitest test file using `@testing-library/react` `renderHook` and `act` to fire `window.innerWidth` changes + `window.dispatchEvent(new Event('resize'))`.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/useBreakpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/useBreakpoint.ts` exports `useBreakpoint` and `Breakpoint` type
    - 5 tests in the test file, all pass
    - `npm run type-check` passes (no new errors)
  </acceptance_criteria>
  <done>Hook exists, tests pass, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Produce 07-MOBILE-AUDIT.md (D-01, D-02)</name>
  <files>.planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md</files>
  <read_first>
    - src/components/pages/ (list all files)
    - src/pages/ (list all files)
    - src/components/layout/PageRouter.tsx to understand routing
    - 07-CONTEXT.md D-06 for the initial classification guess
  </read_first>
  <action>
    Walk `src/components/pages/` and `src/pages/` and classify every page. Write `07-MOBILE-AUDIT.md` with:

    1. Header section explaining the 3 classifications (mobile-native, mobile-view-only, desktop-only) verbatim from D-01.
    2. A table with columns: `| Page | File | Classification | Rationale | Polish Priority |`
       - Polish Priority: P0 (critical-path per D-09), P1 (should-have), P2 (nice-to-have), N/A (desktop-only)
    3. Every page under `src/components/pages/` and `src/pages/` gets a row.
    4. Explicit call-outs for the D-09 critical paths (auth, projects list, matrix view, brainstorm session, settings) — these must be P0 mobile-native or mobile-view-only.
    5. Desktop-only pages get rationale "complex authoring / data-dense / admin per D-06" and N/A priority.
    6. "Downstream work list" section at the bottom listing every P0/P1 page — this is what 07-02 consumes.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md &amp;&amp; grep -q "| Page | File | Classification |" .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md &amp;&amp; grep -q "mobile-native" .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md &amp;&amp; grep -q "desktop-only" .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md &amp;&amp; grep -q "Downstream work list" .planning/phases/07-mobile-polish-video-analysis/07-MOBILE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at specified path
    - Contains the 5-column classification table
    - Every file under `src/components/pages/` has a row (count rows ≥ count of .tsx files in that dir)
    - D-09 critical paths (Auth, ProjectsPage, MatrixPage/ProjectMatrix, BrainstormSession, Settings) each appear with P0 priority
    - "Downstream work list" section exists
  </acceptance_criteria>
  <done>Audit document committed and ready to drive 07-02.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/hooks/__tests__/useBreakpoint.test.ts` passes
- `npm run type-check` passes
- Flags verified true at runtime after dev restart
- Audit file grep checks pass
</verification>

<success_criteria>
All 4 mobile flags enabled + allowlisted, useBreakpoint hook shipped with tests, 07-MOBILE-AUDIT.md exists with full page classification table.
</success_criteria>

<output>
Create `.planning/phases/07-mobile-polish-video-analysis/07-01-SUMMARY.md` on completion.
</output>
