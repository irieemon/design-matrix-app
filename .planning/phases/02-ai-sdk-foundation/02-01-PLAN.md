---
phase: 02-ai-sdk-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/_lib/ai/providers.ts
  - api/_lib/ai/modelRouter.ts
  - api/_lib/ai/utils/prompts.ts
  - api/_lib/ai/utils/parsing.ts
  - api/_lib/ai/utils/tokenTracking.ts
  - api/_lib/ai/__tests__/router.test.ts
  - api/_lib/ai/__tests__/handlers/ideas.test.ts
  - api/_lib/ai/__tests__/handlers/insights.test.ts
  - api/_lib/ai/__tests__/handlers/roadmap.test.ts
  - api/_lib/ai/index.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "AI SDK v6 is installed and importable in the api/ directory"
    - "Model router maps task+capability context to a provider/model selection"
    - "Vision tasks never route to MiniMax (no image support)"
    - "Audio tasks always route to OpenAI whisper-1"
    - "Text-only tasks can route to any available provider"
    - "Token usage format mapping converts camelCase SDK output to snake_case DB format"
  artifacts:
    - path: "api/_lib/ai/providers.ts"
      provides: "Gateway provider factory and getModel() function"
      exports: ["getModel"]
    - path: "api/_lib/ai/modelRouter.ts"
      provides: "Capability-based model selection"
      exports: ["selectModel", "TaskRoutingContext", "ModelSelection"]
    - path: "api/_lib/ai/utils/parsing.ts"
      provides: "Shared JSON extraction from AI responses"
      exports: ["parseJsonResponse"]
    - path: "api/_lib/ai/utils/tokenTracking.ts"
      provides: "Mapping from AI SDK camelCase usage to snake_case tracking"
      exports: ["mapUsageToTracking"]
    - path: "api/_lib/ai/__tests__/router.test.ts"
      provides: "Test stubs for model router capability routing"
  key_links:
    - from: "api/_lib/ai/modelRouter.ts"
      to: "api/_lib/ai/providers.ts"
      via: "selectModel returns modelId used by getModel"
      pattern: "getModel.*selectModel"
    - from: "api/_lib/ai/utils/tokenTracking.ts"
      to: "api/_lib/utils/supabaseAdmin.ts"
      via: "mapUsageToTracking output fed to trackTokenUsage"
      pattern: "prompt_tokens.*completionTokens"

user_setup:
  - service: vercel-ai-gateway
    why: "AI Gateway routes to OpenAI, Anthropic, MiniMax via single endpoint"
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: "Vercel Dashboard -> Settings -> AI Gateway -> Create API Key (or run `vercel env pull` for OIDC)"
      - name: MINIMAX_API_KEY
        source: "MiniMax dashboard -> API Keys (user has MiniMax 2.7 key; needed for gateway BYOK)"
    dashboard_config:
      - task: "Provision AI Gateway API key"
        location: "Vercel Dashboard -> Project Settings -> AI Gateway"
      - task: "Add MINIMAX_API_KEY to Vercel env vars for gateway BYOK routing"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"
---

<objective>
Install AI SDK v6, create provider initialization, model router, shared utilities, and Wave 0 test stubs for the entire phase.

Purpose: Establish the foundation that all subsequent handler migrations depend on -- the SDK, the provider factory, the routing logic, and the test scaffolding that validates no-regression behavior.

Output: `api/_lib/ai/` directory with providers.ts, modelRouter.ts, shared utils, and test stubs for Plans 02 and 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-sdk-foundation/02-CONTEXT.md
@.planning/phases/02-ai-sdk-foundation/02-RESEARCH.md
@.planning/phases/02-ai-sdk-foundation/02-VALIDATION.md

@api/ai.ts
@api/_lib/utils/supabaseAdmin.ts
@api/_lib/services/subscriptionService.ts
@api/_lib/middleware/compose.ts
@src/lib/ai/openaiModelRouter.ts

<interfaces>
<!-- Existing interfaces the executor needs -->

From api/_lib/middleware (do not change):
```typescript
export type AuthenticatedRequest = VercelRequest & { user?: { id: string; email: string } };
export function compose(...middlewares): (handler) => handler;
export function withAuth(req, res, next): void;
export function withCSRF(options?): middleware;
export function withUserRateLimit(options): middleware;
```

From api/_lib/utils/supabaseAdmin.ts:
```typescript
export async function trackTokenUsage(userId: string, usage: {
  prompt_tokens: number;
  completion_tokens: number;
  total_tokens: number;
}): Promise<void>;
```

From api/_lib/services/subscriptionService.ts:
```typescript
export async function checkLimit(userId: string, feature: string): Promise<{
  canUse: boolean;
  current: number;
  limit: number;
  percentageUsed: number;
}>;
export async function trackAIUsage(userId: string, feature: string): Promise<void>;
```

From src/lib/ai/openaiModelRouter.ts (reference only, not imported by api/):
```typescript
export interface TaskContext {
  taskType: string;
  hasImages: boolean;
  hasAudio: boolean;
  expectedOutputSize: 'small' | 'medium' | 'large';
  priority: 'speed' | 'quality' | 'balanced';
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install AI SDK and create provider + utility foundations</name>
  <files>
    package.json,
    api/_lib/ai/providers.ts,
    api/_lib/ai/utils/parsing.ts,
    api/_lib/ai/utils/tokenTracking.ts,
    api/_lib/ai/utils/prompts.ts
  </files>
  <behavior>
    - parseJsonResponse extracts JSON from plain text containing a JSON array or object
    - parseJsonResponse strips markdown ```json code blocks before parsing
    - parseJsonResponse returns parsed object, throws on invalid JSON
    - mapUsageToTracking converts { promptTokens: 10, completionTokens: 5, totalTokens: 15 } to { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 }
    - mapUsageToTracking returns zeros when usage is undefined/null
  </behavior>
  <action>
    1. Run `npm install ai` to install AI SDK v6 (per D-01 gateway approach). Install ONLY the `ai` package. Do NOT install `@ai-sdk/openai`, `@ai-sdk/anthropic`, or `vercel-minimax-ai-provider`. The gateway built into the `ai` package handles all three providers.

    2. Create `api/_lib/ai/providers.ts`:
       - Import `createGateway` from `ai`
       - Export `getModel(gatewayModelId: string)` that lazily creates gateway instance (do NOT create at module top level -- serverless env vars may not be set at import time per research anti-pattern)
       - gatewayModelId format: `'openai/gpt-4o'`, `'anthropic/claude-sonnet-4.5'`, `'minimax/minimax-m2'`
       - Read `AI_GATEWAY_API_KEY` from `process.env` inside the factory
       - No fallback path to separate packages -- gateway is the committed approach per D-01

    3. Create `api/_lib/ai/utils/parsing.ts`:
       - Export `parseJsonResponse(text: string): unknown` -- strips markdown code fences (```json ... ``` or ``` ... ```), then JSON.parse. Throws descriptive error on failure.
       - This consolidates the repeated JSON extraction logic from all 6 handlers in the current ai.ts

    4. Create `api/_lib/ai/utils/tokenTracking.ts`:
       - Export `mapUsageToTracking(usage?: { promptTokens: number; completionTokens: number; totalTokens: number })` that returns `{ prompt_tokens, completion_tokens, total_tokens }` (snake_case for existing `trackTokenUsage`). Returns all zeros if usage is undefined.

    5. Create `api/_lib/ai/utils/prompts.ts`:
       - Extract shared prompt-building functions from current `api/ai.ts`: `getProjectTypePersona()`, any shared persona builders, confidence calculators, and relevance assessors that are used by multiple handlers.
       - Keep handler-specific prompts in the handler files (they move in Plan 02/03).
       - Reference the existing function signatures from `api/ai.ts` -- do NOT change prompt content.
  </action>
  <verify>
    <automated>npm run test:run -- api/_lib/ai/utils/</automated>
  </verify>
  <done>
    - `ai@6.x` appears in package.json dependencies
    - `api/_lib/ai/providers.ts` exports `getModel()` using gateway only (no fallback to separate packages)
    - `api/_lib/ai/utils/parsing.ts` exports `parseJsonResponse()` that strips markdown fences and parses JSON
    - `api/_lib/ai/utils/tokenTracking.ts` exports `mapUsageToTracking()` converting camelCase to snake_case
    - `api/_lib/ai/utils/prompts.ts` exports shared prompt builders extracted from api/ai.ts
    - All utility tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create model router with capability-based routing and Wave 0 test stubs</name>
  <files>
    api/_lib/ai/modelRouter.ts,
    api/_lib/ai/__tests__/router.test.ts,
    api/_lib/ai/__tests__/handlers/ideas.test.ts,
    api/_lib/ai/__tests__/handlers/insights.test.ts,
    api/_lib/ai/__tests__/handlers/roadmap.test.ts,
    api/_lib/ai/index.ts
  </files>
  <behavior>
    - selectModel with hasVision=true returns provider 'openai' or 'anthropic' (never 'minimax')
    - selectModel with hasAudio=true returns provider 'openai' and modelId 'whisper-1'
    - selectModel with task='generate-ideas', no vision, no audio returns a valid text model
    - selectModel with task='generate-insights' returns a capable model (gpt-4o or claude-sonnet-4.5)
    - selectModel with task='generate-roadmap' returns a capable model
    - selectModel with task='analyze-image' always returns a vision-capable model
    - selectModel with task='transcribe-audio' always returns whisper-1
    - All handler test stubs exist but are marked pending/todo (Wave 0 scaffolds)
  </behavior>
  <action>
    1. Create `api/_lib/ai/modelRouter.ts` (per D-07, D-08):
       - Export `TaskRoutingContext` interface: `{ task: 'generate-ideas' | 'generate-insights' | 'generate-roadmap' | 'analyze-file' | 'analyze-image' | 'transcribe-audio', hasVision: boolean, hasAudio: boolean, userTier: 'free' | 'pro' | 'enterprise', complexity?: 'low' | 'medium' | 'high' }`
       - Export `ModelSelection` interface: `{ provider: 'openai' | 'anthropic' | 'minimax', modelId: string, gatewayModelId: string, maxOutputTokens: number, temperature?: number }`
       - Export `selectModel(ctx: TaskRoutingContext): ModelSelection`
       - Routing rules (per D-08 and research Model Capability Matrix):
         - `hasVision: true` or task is `analyze-image` or `analyze-file` -> provider: 'openai', modelId: 'gpt-4o', gatewayModelId: 'openai/gpt-4o'. NEVER route to minimax.
         - `hasAudio: true` or task is `transcribe-audio` -> provider: 'openai', modelId: 'whisper-1', gatewayModelId: 'openai/whisper-1'
         - `generate-ideas` (text) -> primary: openai/gpt-4o
         - `generate-insights` (text) -> primary: openai/gpt-4o
         - `generate-roadmap` (text) -> primary: openai/gpt-4o
       - Error response format: fail-fast with 503 and provider error message (no fallback chain between providers). If selectModel is called with an unknown task, throw a descriptive error.
       - Include `MODEL_CONFIG` constant mapping each task to its model params (temperature, maxOutputTokens)

    2. Create `api/_lib/ai/__tests__/router.test.ts`:
       - Test each routing rule from the behavior list above
       - Test that vision tasks NEVER return minimax
       - Test that audio tasks ALWAYS return openai/whisper-1
       - Test all 6 task types return valid ModelSelection objects
       - These are real tests, not stubs -- router is a pure function, perfect for TDD

    3. Create Wave 0 test stubs (per 02-VALIDATION.md Wave 0 Requirements):
       - `api/_lib/ai/__tests__/handlers/ideas.test.ts` -- describe block with pending tests: "returns same JSON shape as legacy handler", "calls checkLimit before generation", "tracks token usage after generation"
       - `api/_lib/ai/__tests__/handlers/insights.test.ts` -- describe block with pending tests: "returns same JSON shape as legacy handler", "handles Anthropic fallback"
       - `api/_lib/ai/__tests__/handlers/roadmap.test.ts` -- describe block with pending tests: "returns same JSON shape as legacy handler", "handles generate-roadmap-v2 action"
       - Mark each test with `it.todo()` -- these stubs are completed in Plans 02 and 03

    4. Create `api/_lib/ai/index.ts` barrel export:
       - Re-export from providers.ts, modelRouter.ts
       - Handler exports will be added in Plans 02 and 03
  </action>
  <verify>
    <automated>npm run test:run -- api/_lib/ai/__tests__/router.test.ts</automated>
  </verify>
  <done>
    - modelRouter.ts exports selectModel with full capability routing
    - Router tests pass: vision never routes to MiniMax, audio always routes to whisper-1, all 6 tasks produce valid selections
    - Wave 0 test stubs exist for ideas, insights, roadmap handlers (it.todo)
    - Barrel index.ts exports providers and router
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API handler -> AI Gateway | Outbound requests carry API keys to external AI providers |
| Client -> API | Untrusted input crosses here (handled by existing middleware, unchanged per D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | api/_lib/ai/providers.ts | mitigate | API keys read from process.env only (never hardcoded); gateway key stored as Vercel env var, not in source. Verify no key appears in error messages or logs. |
| T-02-02 | Information Disclosure | api/_lib/ai/modelRouter.ts | accept | Router is server-side only; model IDs are not secrets. No user data passes through the router. |
| T-02-03 | Elevation of Privilege | api/_lib/ai/providers.ts | mitigate | getModel() is called only from authenticated handlers behind withAuth middleware (D-03 preserves existing middleware chain). No direct client access to provider factory. |
| T-02-04 | Denial of Service | api/_lib/ai/providers.ts | accept | Rate limiting already applied by withUserRateLimit middleware (preserved per D-03). AI Gateway has its own rate limits. |
</threat_model>

<verification>
Per 02-VALIDATION.md sampling rate:
- After Task 1: `npm run test:run -- api/_lib/ai/utils/`
- After Task 2: `npm run test:run -- api/_lib/ai/__tests__/router.test.ts`
- After plan completion: `npm run test:run` (full suite green)
</verification>

<success_criteria>
- `ai` package installed and importable
- `getModel('openai/gpt-4o')` returns a valid AI SDK model instance via gateway (no separate provider packages)
- `selectModel()` enforces capability routing: vision != minimax, audio = whisper-1
- Shared utilities (parsing, token tracking, prompts) are tested and ready for handler use
- Wave 0 test stubs exist for all handlers migrated in Plans 02 and 03
- Full test suite passes with no regressions from SDK installation
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-sdk-foundation/02-01-SUMMARY.md`
</output>
