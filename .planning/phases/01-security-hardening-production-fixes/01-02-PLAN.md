---
phase: 01-security-hardening-production-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/multiModalProcessor.ts
  - api/_lib/middleware/cookies.ts
  - api/_lib/services/subscriptionService.ts
  - src/lib/ai/aiInsightsService.ts
  - src/lib/ai/openaiModelRouter.ts
  - src/lib/ai/intelligentMockData.ts
autonomous: true
requirements:
  - SEC-06
  - SEC-07
  - SEC-08
  - SEC-09
  - SEC-10

must_haves:
  truths:
    - "No undefined error variable crash in multiModalProcessor.ts catch blocks"
    - "Refresh token cookie path is /api so all API endpoints can initiate refresh"
    - "Subscription service returns canUse: false when any error occurs during limit check"
    - "AI insights service reads userTier from user context, not hardcoded pro"
    - "Multi-modal processor returns error responses, not placeholder fake data"
  artifacts:
    - path: "src/lib/multiModalProcessor.ts"
      provides: "Fixed error handling and no placeholder data"
      contains: "catch (error)"
    - path: "api/_lib/middleware/cookies.ts"
      provides: "Cookie path set to /api for refresh token"
      contains: "path: '/api'"
    - path: "api/_lib/services/subscriptionService.ts"
      provides: "Fail-closed subscription enforcement"
      contains: "canUse: false"
    - path: "src/lib/ai/aiInsightsService.ts"
      provides: "Dynamic userTier from context"
  key_links:
    - from: "api/_lib/services/subscriptionService.ts"
      to: "all API rate-limited endpoints"
      via: "subscription check in request flow"
      pattern: "canUse: false"
    - from: "api/_lib/middleware/cookies.ts"
      to: "browser cookie storage"
      via: "Set-Cookie header with path=/api"
      pattern: "path.*'/api'"
---

<objective>
Fix five production bugs in crash-severity order per D-13: undefined error variable (SEC-07), cookie path (SEC-08), fail-open subscription (SEC-06), hardcoded userTier (SEC-09), and placeholder data removal (SEC-10).

Purpose: These bugs range from runtime crashes to security bypasses to data integrity issues. The subscription fail-open bug (SEC-06) is especially dangerous -- it grants unlimited access when the subscription check errors out. All five fixes are localized and well-understood.

Output: Five targeted fixes across 6 files, each addressing a specific documented bug with no behavioral side effects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/multiModalProcessor.ts
@api/_lib/middleware/cookies.ts
@api/_lib/services/subscriptionService.ts
@src/lib/ai/aiInsightsService.ts
@src/lib/ai/openaiModelRouter.ts

<interfaces>
<!-- Bug locations verified from RESEARCH.md with exact line numbers -->

multiModalProcessor.ts line 113-118:
  catch (_error) { ... references bare `error` which is undefined }

cookies.ts line 118:
  path: '/api/auth'  (too narrow, should be '/api')
cookies.ts line 145:
  clearAuthCookies also uses '/api/auth' path

subscriptionService.ts lines 29, 73, 117:
  catch blocks return { canUse: true, ... } (fail-open)

aiInsightsService.ts lines 182, 925:
  userTier: 'pro' hardcoded

openaiModelRouter.ts line 62:
  userTier default 'pro'

intelligentMockData.ts lines 370, 524:
  userTier: 'pro' hardcoded
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix crash bugs and fail-closed subscription (SEC-07, SEC-08, SEC-06)</name>
  <read_first>
    - src/lib/multiModalProcessor.ts (full file -- find all catch blocks with _error/error mismatch)
    - api/_lib/middleware/cookies.ts (full file -- find all cookie path settings)
    - api/_lib/services/subscriptionService.ts (full file -- find all catch blocks returning canUse: true)
  </read_first>
  <files>
    src/lib/multiModalProcessor.ts
    api/_lib/middleware/cookies.ts
    api/_lib/services/subscriptionService.ts
  </files>
  <action>
**Fix 1: SEC-07 -- Undefined error variable in multiModalProcessor.ts**

In `multiModalProcessor.ts`, find all catch blocks where the parameter is `_error` but the body references bare `error`. The known instance is at lines 113-118. Rename the catch parameter from `_error` to `error` so the body can reference it. Search the entire file for other instances of this pattern:
```
grep -n "catch.*_error" src/lib/multiModalProcessor.ts
```
Fix every occurrence where `_error` is caught but `error` is referenced in the body.

**Fix 2: SEC-08 -- Refresh token cookie path too narrow**

In `api/_lib/middleware/cookies.ts`:

1. Find the line setting refresh token cookie path (line ~118). Change:
   ```typescript
   path: '/api/auth'
   ```
   to:
   ```typescript
   path: '/api'
   ```

2. Find `clearAuthCookies()` function (line ~145) which also uses `/api/auth` path. Change to `/api`.

3. Search for any other occurrences of `'/api/auth'` as a path value in this file and update them to `'/api'`.

Do NOT change the access token cookie path if it's set differently (access token may use `/` which is correct).

**Fix 3: SEC-06 -- Subscription service fails closed on errors**

In `api/_lib/services/subscriptionService.ts`, find ALL catch blocks that return `canUse: true` (research identified 3 at lines ~29, ~73, ~117). Change each one:

BEFORE:
```typescript
} catch (error) {
  console.error('Error in checkLimit:', error)
  return { canUse: true, current: 0, limit: 999999, percentageUsed: 0, isUnlimited: true }
}
```

AFTER:
```typescript
} catch (error) {
  console.error('Error in checkLimit:', error)
  return { canUse: false, current: 0, limit: 0, percentageUsed: 100, isUnlimited: false }
}
```

Search the ENTIRE file for `canUse: true` in catch blocks. There may be more than 3. Every error path must return `canUse: false`. Verify: `grep -n "canUse: true" api/_lib/services/subscriptionService.ts` should return ZERO results inside catch blocks after the fix.
  </action>
  <verify>
    <automated>cd "/Users/sean.mcinerney/Documents/claude projects/workshop/design-matrix-app" && echo "=== SEC-07: No _error/error mismatch ===" && grep -n "catch.*_error" src/lib/multiModalProcessor.ts | head -10 && echo "=== SEC-08: Cookie path ===" && grep -n "path:" api/_lib/middleware/cookies.ts | head -10 && echo "=== SEC-06: No canUse:true in catch ===" && grep -B2 -A2 "canUse: true" api/_lib/services/subscriptionService.ts | head -30 && echo "=== TypeScript check ===" && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/multiModalProcessor.ts contains NO `catch (_error)` blocks where body references bare `error` (all renamed to `catch (error)`)
    - api/_lib/middleware/cookies.ts contains `path: '/api'` for refresh token cookie (NOT `path: '/api/auth'`)
    - api/_lib/middleware/cookies.ts clearAuthCookies uses `path: '/api'` (NOT `path: '/api/auth'`)
    - api/_lib/services/subscriptionService.ts contains ZERO instances of `canUse: true` inside catch blocks
    - Every catch block in subscriptionService.ts returns `canUse: false`
    - TypeScript compilation succeeds with no errors in modified files
  </acceptance_criteria>
  <done>Three crash/security bugs fixed: error variable renamed (SEC-07), cookie path widened to /api (SEC-08), subscription service fails closed on all error paths (SEC-06). No canUse:true remains in any catch block.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded userTier and remove placeholder data (SEC-09, SEC-10)</name>
  <read_first>
    - src/lib/ai/aiInsightsService.ts (full file -- find all hardcoded userTier: 'pro')
    - src/lib/ai/openaiModelRouter.ts (find userTier default)
    - src/lib/ai/intelligentMockData.ts (find hardcoded userTier references)
    - src/lib/multiModalProcessor.ts (find placeholder return strings for OCR, video, audio)
  </read_first>
  <files>
    src/lib/ai/aiInsightsService.ts
    src/lib/ai/openaiModelRouter.ts
    src/lib/ai/intelligentMockData.ts
    src/lib/multiModalProcessor.ts
  </files>
  <action>
**Fix 4: SEC-09 -- Hardcoded userTier in AI services**

The hardcoded `userTier: 'pro'` gives all users pro-tier AI access regardless of their actual subscription.

1. In `src/lib/ai/aiInsightsService.ts` at lines ~182 and ~925: replace `userTier: 'pro'` with `userTier: userTier` or `userTier: tier` reading from the function parameter or caller context. Trace the call chain to find where userTier should come from:
   - Check the function signatures that contain the hardcoded value
   - If the function already accepts a `userTier` parameter, use it
   - If not, add `userTier: string` parameter with default `'free'` (fail-safe default -- free tier, not pro)
   - The caller must pass the user's actual tier from their subscription/profile data

2. In `src/lib/ai/openaiModelRouter.ts` at line ~62: find the `userTier` default. If it defaults to `'pro'`, change default to `'free'`. This ensures unrecognized users get free-tier model routing, not pro-tier.

3. In `src/lib/ai/intelligentMockData.ts` at lines ~370 and ~524: these are mock/test data generators. Replace hardcoded `'pro'` with a parameter or use `'free'` as the default. If these are only used in development/testing, mark them clearly with a comment: `// Mock data -- uses free tier by default`.

**Fix 5: SEC-10 -- Remove multi-modal placeholder returns**

In `src/lib/multiModalProcessor.ts`, find the placeholder return strings:
- OCR placeholder (line ~149): returns fake OCR text like "Extracted text from image..."
- Video transcription placeholder (line ~187): returns fake video analysis
- Audio transcription placeholder (line ~221): returns fake audio transcription

Replace each placeholder with a proper error response. Use this pattern (Claude's discretion per CONTEXT.md):
```typescript
return {
  success: false,
  error: 'Feature not yet available. Multi-modal analysis requires AI SDK setup.',
  data: null
}
```

Or if the return type doesn't support an error field, throw an appropriate error:
```typescript
throw new Error('Multi-modal analysis is not yet available. This feature will be enabled in a future update.')
```

The key requirement: NO function in multiModalProcessor.ts should return fake/placeholder content strings that look like real analysis results. Search for placeholder indicator strings: `grep -n "placeholder\|Extracted text\|sample\|mock\|TODO\|PLACEHOLDER" src/lib/multiModalProcessor.ts`
  </action>
  <verify>
    <automated>cd "/Users/sean.mcinerney/Documents/claude projects/workshop/design-matrix-app" && echo "=== SEC-09: No hardcoded pro tier ===" && grep -n "userTier.*['\"]pro['\"]" src/lib/ai/aiInsightsService.ts src/lib/ai/openaiModelRouter.ts src/lib/ai/intelligentMockData.ts 2>/dev/null | head -20 && echo "=== SEC-10: No placeholder data ===" && grep -ni "placeholder\|extracted text from\|sample.*transcription\|mock.*analysis" src/lib/multiModalProcessor.ts | head -20 && echo "=== TypeScript check ===" && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "userTier.*'pro'" src/lib/ai/aiInsightsService.ts` returns ZERO matches (no hardcoded pro tier)
    - `grep -n "userTier.*'pro'" src/lib/ai/openaiModelRouter.ts` returns ZERO matches
    - openaiModelRouter.ts default userTier is `'free'` (not `'pro'`)
    - src/lib/multiModalProcessor.ts contains NO placeholder/fake content strings for OCR, video, or audio
    - Multi-modal functions return error responses or throw errors when features are unavailable (not fake data)
    - TypeScript compilation succeeds with no errors in modified files
    - intelligentMockData.ts uses `'free'` as default tier or accepts tier as parameter
  </acceptance_criteria>
  <done>Hardcoded userTier replaced with dynamic values defaulting to 'free' across all AI services (SEC-09). All multi-modal placeholder returns replaced with proper error responses (SEC-10). No fake data remains in production code paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subscription check -> feature access | Error in subscription check must NOT grant access |
| Cookie scope -> API endpoints | Refresh token cookie must be readable by all API paths |
| AI service -> model selection | User tier determines model access level |
| Multi-modal API -> client | Must never return fake data that looks like real analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | subscriptionService.ts | mitigate | Change all catch blocks to return `canUse: false` (fail-closed). Zero instances of `canUse: true` in error paths. |
| T-02-02 | Elevation of Privilege | aiInsightsService.ts, openaiModelRouter.ts | mitigate | Replace hardcoded `userTier: 'pro'` with dynamic value from user context, default to `'free'` |
| T-02-03 | Information Disclosure | multiModalProcessor.ts | mitigate | Remove placeholder strings that could mislead users into thinking analysis was performed |
| T-02-04 | Denial of Service | multiModalProcessor.ts (SEC-07) | mitigate | Fix undefined error variable that causes runtime crash in catch block |
| T-02-05 | Spoofing | cookies.ts (SEC-08) | mitigate | Widen refresh token cookie path from `/api/auth` to `/api` so token refresh works from all API endpoints, preventing unexpected logouts |
</threat_model>

<verification>
1. `grep -rn "catch.*_error" src/lib/multiModalProcessor.ts` returns no mismatched catch/body references
2. `grep -n "'/api/auth'" api/_lib/middleware/cookies.ts` returns ZERO matches (all updated to `/api`)
3. `grep -n "canUse: true" api/_lib/services/subscriptionService.ts` shows ZERO instances in catch blocks
4. `grep -rn "userTier.*'pro'" src/lib/ai/` returns ZERO matches
5. `grep -ni "placeholder\|extracted text" src/lib/multiModalProcessor.ts` returns ZERO matches
6. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- SEC-07: No undefined error variable crash in multiModalProcessor.ts
- SEC-08: Refresh token cookie path is `/api` (readable by all API endpoints)
- SEC-06: Subscription service returns `canUse: false` on every error path
- SEC-09: All AI services read userTier from context with `'free'` default
- SEC-10: No placeholder/fake data returned from any multi-modal function
- All five fixes verified by grep and TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening-production-fixes/01-02-SUMMARY.md`
</output>
