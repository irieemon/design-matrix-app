# Phase 04 — Continue Here

**Saved:** 2026-04-07
**Last commit:** 6f3b9cd
**Status:** Mid-checkpoint — Plan 04-02 Task 3 (iOS Safari human-verify) in progress

---

## Where We Are

Phase 4 execution is underway. Plan 04-01 is complete. Plan 04-02 Tasks 1 + 2 are complete (committed). We are mid-checkpoint on Task 3 (MOB-02 manual iOS Safari verification).

### What's been built (committed)
- `src/hooks/useAudioRecorder.ts` — MediaRecorder hook with iOS mp4 fallback + mic cleanup
- `src/hooks/__tests__/useAudioRecorder.test.ts` — 9 tests, all green
- `src/lib/audioTranscription.ts` — validate (25MB, MIME) + normalizeTranscriptionResult
- `src/lib/__tests__/audioTranscription.test.ts` — 8 tests, all green
- `src/test/mocks/mediaRecorder.ts` — shared vitest MediaRecorder mock
- `src/components/AIIdeaModal.tsx` — Audio tab added (3rd tab, Record + Upload, AudioStage machine, step indicators, review panel, Create Idea)
- `src/components/__tests__/AIIdeaModal.audio.test.tsx` — 10 tests, all green
- `api/_lib/middleware/withCSRF.ts` — DEV_TUNNEL_URL env var support added

### Fixes applied during mobile testing
- `src/lib/audioTranscription.ts`: strip codec params from MIME type before validation (`audio/mp4;codecs=...` → `audio/mp4`)
- `vite.config.ts`: allowedHosts for ngrok tunnel
- `api/_lib/middleware/withCSRF.ts`: DEV_TUNNEL_URL in allowed origins

---

## Current Blocker

**Transcription returning 500 on Vercel production.**

Testing on device via Vercel deploy (pushed to GitHub, auto-deployed). The 500 happens on `POST /api/ai?action=transcribe-audio`.

### Most likely cause
`OPENAI_API_KEY` not set in Vercel environment variables.

**To diagnose:**
```bash
vercel logs --prod | grep -A5 "transcribe-audio"
```
Or: Vercel dashboard → project → Functions → `/api/ai` invocation → read error.

**To fix if missing API key:**
Vercel dashboard → Settings → Environment Variables → add `OPENAI_API_KEY`.

---

## Known Bugs (found during mobile testing, not yet fixed)

1. **Modal doesn't scroll on mobile** — the review panel renders below the step indicators but the modal has no scroll, so it's clipped/invisible. After fixing the 500, if the transcription succeeds, the review panel won't be visible on iPhone.
   - Fix: add `overflow-y-auto` to the modal body or the Audio tab content container in `src/components/AIIdeaModal.tsx`

2. **Step indicators have no active/done visual styling on mobile** — the `data-state` attribute CSS may not be generating Tailwind classes for `data-state="done"` / `data-state="active"` variants.

---

## Critical Anti-Patterns

| # | Pattern | Severity | How it manifested |
|---|---------|----------|-------------------|
| 1 | Worktree isolation breaks when .planning/ not tracked at base commit | blocking | First executor agent spawned in worktree couldn't find .planning/ — had to disable worktrees and run sequentially |

---

## Next Steps (in order)

1. **Fix the 500** — check Vercel logs / env vars
2. **Fix modal scroll** — add overflow-y-auto to modal body in AIIdeaModal.tsx
3. **Re-test on device** — confirm full flow: record → upload → transcribe → review panel → Create Idea
4. **Respond "approved" to the checkpoint** — then spawn continuation agent to complete Plan 04-02
5. **Run phase verification** — `/gsd-verify-work 4`

---

## How to Resume

```
/clear
/gsd-execute-phase 4
```

The orchestrator will detect Plan 04-02 has no SUMMARY.md yet (Task 3 checkpoint is pending) and resume from the checkpoint.

Or to go straight to the checkpoint continuation after fixing the 500 + scroll bug, type "approved" when the checkpoint is presented.
