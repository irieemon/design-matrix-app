---
phase: 05.4a-session-scope-voting
milestone: v1.1
status: in_progress
units_complete: 1
units_total: 5
last_updated: 2026-04-09T20:30:00Z
orchestration: eva-pipeline
requirements_in_progress: [COLLAB-01-session, COLLAB-05-UI, COLLAB-06]
---

# Phase 05.4a: Session-Scope Voting — Summary

Running summary. Updated as each wave completes.

## Wave 1 — Unit 1: voteRepository D-07 fix ✅

**Commit:** `dc7c01b`
**Agent:** Colby (TDD)
**LOC:** +124 / -9
**Files:**
- `src/lib/repositories/voteRepository.ts` — `VoteRepositoryError` class + `removeVote` throws
- `src/lib/repositories/__tests__/voteRepository.test.ts` — 7 spec tests + 2 error class tests + 1 pre-existing

**What shipped:**
- New `VoteRepositoryError extends Error` with `name = 'VoteRepositoryError'`, `cause` field, `Object.setPrototypeOf` fix for transpilation prototype chain
- `removeVote` converted from 3 silent-swallow paths (auth failure, supabase error, outer catch) to typed throws
- `castVote` signature and return shape unchanged (frozen per D-03)

**Test results:** 10/10 passing. No regressions in `src/lib/repositories`. `npx tsc --noEmit` clean for voteRepository.

### QA verdicts

| Agent | Verdict | Notes |
|-------|---------|-------|
| Roz (scoped QA) | ✅ PASS | All 3 silent paths eliminated; prototype chain fix correct; D-07 compliance verified |
| Poirot (blind diff) | ⚠ 6 findings | Triaged below |

### Poirot finding triage

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 1 | FIX-REQUIRED | `readonly cause: unknown` shadows `Error.cause` on ES2020 target | **DEFER** — tracked NIT, no serializers read `Error.cause` in this codebase |
| 2 | FIX-REQUIRED | Mock chain returns `{ error }` without `data: null` | **DEFER** — test-only, no behavioral impact |
| 3 | FIX-REQUIRED | T-054A-002 double-invocation conflates two assertions | **DEFER** — Roz also noted; correct but verbose |
| 4 | FIX-REQUIRED | Orphan throw — no production caller catches `VoteRepositoryError` | **DISMISS** — Unit 3 (`useDotVoting` hook) is the caller, explicitly queued. Poirot blind to unit roadmap. |
| 5 | NIT | `options?: { cause?: unknown }` vs standard `ErrorOptions` type | **DEFER** — cosmetic |
| 6 | NIT | Test IDs in `it()` names vs comments | **DEFER** — convention drift, not correctness |

**5 deferred NITs tracked:** to be addressed in Unit 3 or Phase 05.5 cleanup. None affect Unit 1 behavioral correctness.

### Process deviations

- **Colby committed directly** instead of routing through Ellis (Eva's instruction included "Atomic commit at end" which overrode gate 2). Noted for future units — Ellis should commit per-unit from Wave 2 onward.

### Budget actual

| Step | Tokens |
|------|--------|
| Colby Unit 1 | ~46k |
| Roz Unit 1 QA | ~39k |
| Poirot Unit 1 | ~38k |
| **Wave 1 total** | **~123k** |

Under estimate (40-70k Colby budget held; QA overhead ~77k).

---

## Wave 2 — Units 2 + 3 (batched) ✅

**Commits:** `1f601fd` (build) + `36ae490` (Poirot Finding 2 followup fix)
**Agent:** Colby (TDD)
**LOC:** ~1565 total across 6 files (build) + 18 LOC (fix followup)

**Files shipped:**
- `src/lib/realtime/ScopedRealtimeManager.ts` — NEW sibling class per D-09 (~280 LOC)
- `src/lib/realtime/__tests__/ScopedRealtimeManager.test.ts` — 15 tests
- `src/lib/realtime/__tests__/multiClient.test.ts` — 5 tests (replaced Wave 0 stub)
- `src/hooks/useDotVoting.ts` — voting hook with optimistic UI + rollback (~220 LOC + polling tick fix)
- `src/hooks/__tests__/useDotVoting.test.ts` — 16 tests (15 original + T-054A-062b polling)
- `src/contexts/DotVotingContext.tsx` — provider shell (~55 LOC)

**Test results:** 36/36 passing (15 + 5 + 16). Regression guard held: `BrainstormRealtimeManager.simple.test.ts` 18/18 green; `BrainstormRealtimeManager.test.ts` 3/25 passing (22 pre-existing failures unchanged).

**D-09 verified mechanically:** `grep BrainstormRealtimeManager src/lib/realtime/ScopedRealtimeManager.ts` → 2 hits, both in JSDoc comment. Zero runtime coupling. useDotVoting.ts and DotVotingContext.tsx have zero references.

### QA verdicts

| Agent | Verdict | Notes |
|-------|---------|-------|
| Roz (full wave QA) | ✅ PASS | D-09 mechanical compliance, regression guard held, 35/35 green, Poirot Finding 4 closure verified |
| Poirot (blind diff) | ⚠ 7 findings | Triaged below — 1 fixed (36ae490), 3 deferred to Wave 3, 1 dismissed, 2 NITs |

### Poirot Wave 2 finding triage

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 1 | MUST-FIX | `useDotVoting` DELETE classifier uses heuristic (`old === null && empty new`) instead of `payload.eventType === 'DELETE'`; would misroute UPDATE events | **DEFER → Wave 3** — `idea_votes` has no UPDATE path in current schema; latent risk only if mutable votes added later |
| 2 | MUST-FIX | `useDotVoting` never registers `onPollingTick(reconcile)` — hook goes dark when manager degrades to polling state | **FIXED** in 36ae490 — 5 LOC + 13 LOC test (T-054A-062b); 16/16 passing |
| 3 | MUST-FIX | `removeVote` doesn't update `votesUsedRef.current` synchronously like `castVote` does; two rapid removeVotes could rollback to stale value | **DEFER → Wave 3** — rare concurrency edge case |
| 4 | MUST-FIX | `resubscribe()` race with `unsubscribe()` could call `supabase.removeChannel(null)` | **DEFER → Wave 3** — low-probability timing race, defensive fix is cheap |
| 5 | MUST-FIX | Orphan `DotVotingProvider` — no consumers outside the file | **DISMISS** — expected Unit 4/5 scope (same class as Wave 1 Finding 4) |
| 6 | NIT | `reconcile()` swallows errors, never surfaces to `error` state | **DEFER → 05.5** — cosmetic |
| 7 | NIT | `multiClient.test.ts` `SharedChannelBus.track` peer failure propagation | **DEFER → 05.5** — test-only |

### Budget actual

| Step | Tokens |
|------|--------|
| Colby Wave 2 build | ~121k |
| Roz Wave 2 QA | ~65k |
| Poirot Wave 2 | ~53k |
| Colby Finding 2 surgical fix | ~55k |
| **Wave 2 total** | **~294k** |

---

## Wave 3 — Units 4 + 5 + Poirot cleanup ⏭ QUEUED

- **Unit 4:** 3 components (`DotVoteControls`, `DotBudgetIndicator`, `SessionPresenceStack`) (~900 LOC, splittable 4a/4b)
- **Unit 5:** Wire-up in `MatrixFullScreenView` (~50 LOC)
- **Wave 2 deferred cleanup** (bundle into Wave 3 Colby context while in the area):
  - Poirot Finding 1: DELETE classifier via `payload.eventType`
  - Poirot Finding 3: `removeVote` sync ref update
  - Poirot Finding 4: `resubscribe()` null-channel guard

## Deferred NIT cleanup list (carry to Unit 3 or 05.5)

1. Bump `lib` to ES2022 and use native `Error.cause` (Finding 1)
2. Align mock shape to `{ data: null, error }` (Finding 2)
3. Split T-054A-002 into two tests (Finding 3)
4. Use `ErrorOptions` type (Finding 5)
5. Move T-054A-* IDs from test names to comments (Finding 6)
