---
phase: 05.1-legacy-collaborationservice-migration-to-phase-5-schema
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/lib/database.ts
  - src/lib/services/CollaborationService.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "CollaborationService has no method whose name starts with 'legacy'"
    - "database.ts has no reference to CollaborationService.legacy*"
    - "database.ts collaboration wrappers with zero callers are deleted"
  artifacts:
    - path: "src/lib/services/CollaborationService.ts"
      provides: "Phase-5-only CollaborationService (no legacy* methods)"
    - path: "src/lib/database.ts"
      provides: "Updated DatabaseService — dead collab wrappers removed"
  key_links:
    - from: "src/lib/database.ts"
      to: "CollaborationService (Phase-5 methods)"
      via: "direct call OR wrapper deleted"
      pattern: "CollaborationService\\.(?!legacy)"
---

<objective>
Delete all `legacy*` methods from `CollaborationService.ts` and update/remove the matching delegation wrappers in `database.ts`. After this plan, `grep -n "legacy" src/lib/services/CollaborationService.ts` returns nothing, and no file in `src/` references `CollaborationService.legacy*`.

Purpose: Physical removal of the legacy layer — the core deliverable of this phase.
Output: CollaborationService is Phase-5-only; database.ts is trimmed of dead wrappers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-CONTEXT.md
@.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-01-SUMMARY.md
@src/lib/services/CollaborationService.ts
@src/lib/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy* methods from CollaborationService.ts</name>
  <files>src/lib/services/CollaborationService.ts</files>
  <read_first>
    - src/lib/services/CollaborationService.ts lines 487-574 (legacy block + serviceResultToApiResponse)
  </read_first>
  <action>
    Delete lines ~488-550 covering these six methods entirely (including their JSDoc comments):
    - `legacyAddProjectCollaborator`
    - `legacyGetProjectCollaborators`
    - `legacyRemoveProjectCollaborator`
    - `legacyUpdateCollaboratorRole`
    - `legacyGetUserProjectRole`
    - `legacyCanUserAccessProject`
    KEEP `serviceResultToApiResponse` (not a legacy* method — separate utility).
    Do NOT touch Phase-5 methods at lines 50-486.
  </action>
  <verify>
    <automated>grep -n "legacy" src/lib/services/CollaborationService.ts; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "legacy" src/lib/services/CollaborationService.ts` returns nothing (exit 1).
    - `grep -n "serviceResultToApiResponse" src/lib/services/CollaborationService.ts` returns 1 match (still present).
    - Phase-5 methods `addProjectCollaborator`, `getProjectCollaborators`, `removeProjectCollaborator`, `updateCollaboratorRole`, `getUserProjectRole`, `canUserAccessProject`, `subscribeToProjectCollaborators` all still present (`grep -c` ≥ 1 each).
  </acceptance_criteria>
  <done>File contains only Phase-5 methods + serviceResultToApiResponse utility.</done>
</task>

<task type="auto">
  <name>Task 2: Update/remove database.ts collaboration wrappers</name>
  <files>src/lib/database.ts</files>
  <read_first>
    - src/lib/database.ts lines 253-345 (collaboration delegation block)
    - Run `grep -rn "DatabaseService\.\(addProjectCollaborator\|getProjectCollaborators\|removeProjectCollaborator\|updateCollaboratorRole\|getUserProjectRole\|canUserAccessProject\|subscribeToProjectCollaborators\)" src/ --include='*.ts' --include='*.tsx'` to enumerate remaining callers (excluding `src/lib/database.ts` itself).
  </read_first>
  <action>
    For each of the six collaboration wrappers in `database.ts` lines ~259-319 (addProjectCollaborator, getProjectCollaborators, removeProjectCollaborator, updateCollaboratorRole, getUserProjectRole, canUserAccessProject) AND the subscribeToProjectCollaborators wrapper at ~333:

    1. Count non-test callers using the grep from read_first.
    2. If ZERO non-test callers remain: DELETE the wrapper entirely (per D-03).
    3. If test-only callers remain (e.g. `src/lib/__tests__/database.test.ts` — will be handled by Plan 03): DELETE the wrapper anyway. Plan 03 will rewrite that test file to not depend on it.
    4. If production callers remain that were missed in Plan 01: STOP and report — this is a Plan 01 gap, not a Plan 02 task.

    Expected outcome (based on Plan 01 migration): all six collaboration wrappers + subscribeToProjectCollaborators wrapper are DELETED from database.ts. Remove any now-unused `CollaborationService` import if no other usage remains.

    Do NOT touch the ProjectService delegations at lines ~180-230 (out of scope — those are `legacyGetAllProjects` etc., a separate ProjectService migration).
  </action>
  <verify>
    <automated>grep -n "CollaborationService\.legacy\|legacyAddProjectCollaborator\|legacyGetProjectCollaborators\|legacyRemoveProjectCollaborator\|legacyUpdateCollaboratorRole\|legacyGetUserProjectRole\|legacyCanUserAccessProject" src/lib/database.ts; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CollaborationService\.legacy" src/lib/database.ts` returns nothing.
    - `grep -rn "legacyAdd\|legacyGet\|legacyRemove\|legacyUpdate\|legacyCan" src/ --include='*.ts' --include='*.tsx'` matches ONLY inside `src/lib/services/ProjectService.ts` (ProjectService legacy* — out of scope) and test files (handled in Plan 03).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>database.ts no longer delegates to CollaborationService.legacy*; typecheck green; any dead collaboration wrappers removed.</done>
</task>

</tasks>

<verification>
- `grep -n "legacy" src/lib/services/CollaborationService.ts` → empty.
- `grep -rn "CollaborationService\.legacy" src/` → empty.
- `npm run type-check` exits 0.
</verification>

<success_criteria>
Physical legacy layer deleted from CollaborationService and database.ts. Only remaining legacy* references in `src/` are inside test files (Plan 03) and the unrelated ProjectService.legacy* methods.
</success_criteria>

<output>
Create `.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-02-SUMMARY.md` listing deleted methods and deleted database.ts wrappers.
</output>
