---
phase: 05.1-legacy-collaborationservice-migration-to-phase-5-schema
plan: 03
type: execute
wave: 3
depends_on: ["05.1-02"]
files_modified:
  - src/lib/services/__tests__/CollaborationService.test.ts
  - src/lib/__tests__/database.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "CollaborationService.test.ts has no references to legacy* methods"
    - "Tests cover Phase-5 schema: invitations table, projects.owner_id, viewer/editor roles"
    - "database.test.ts collaboration tests are removed or rewritten to not depend on deleted wrappers"
    - "grep -rn 'legacyAdd|legacyGet|legacyRemove|legacyUpdate|legacyCan' src/ returns nothing"
    - "npm run type-check exits 0"
    - "vitest run on CollaborationService.test.ts passes"
    - "Phase 5 invitation flow still works E2E (human-verified)"
  artifacts:
    - path: "src/lib/services/__tests__/CollaborationService.test.ts"
      provides: "Rewritten test suite against Phase-5 schema"
    - path: "src/lib/__tests__/database.test.ts"
      provides: "database.test.ts with dead collaboration tests removed"
  key_links:
    - from: "CollaborationService.test.ts"
      to: "Phase-5 invitations/projects.owner_id conventions"
      via: "test fixtures matching 05-01-schema-and-fixtures-PLAN.md"
      pattern: "invitations|owner_id"
---

<objective>
Rewrite `CollaborationService.test.ts` against the Phase-5 schema and remove/rewrite dead tests in `database.test.ts`. Final verification: zero `legacy*` references anywhere in `src/` (except the unrelated ProjectService namespace), typecheck green, tests green, and human-verified Phase 5 invitation flow works end-to-end.

Purpose: Deliver D-06, D-07, D-08, D-09, D-10 — the Definition of Done for this phase.
Output: Green test suite + signed-off E2E invitation flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-CONTEXT.md
@.planning/phases/05-real-time-collaboration/05-01-schema-and-fixtures-PLAN.md
@.planning/phases/05-real-time-collaboration/05-02-invitations-backend-PLAN.md
@src/lib/services/__tests__/CollaborationService.test.ts
@src/lib/services/CollaborationService.ts
@src/lib/__tests__/database.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite CollaborationService.test.ts against Phase-5 schema</name>
  <files>src/lib/services/__tests__/CollaborationService.test.ts</files>
  <read_first>
    - src/lib/services/__tests__/CollaborationService.test.ts (full file — understand current mocking/fixture pattern)
    - .planning/phases/05-real-time-collaboration/05-01-schema-and-fixtures-PLAN.md (Phase-5 schema + test fixture conventions)
    - src/lib/services/CollaborationService.ts (Phase-5 method signatures at lines 50-486)
    - Any existing Phase-5 invitation test file to match mocking style (grep: `grep -rln "invitations" src/**/__tests__/`)
  </read_first>
  <behavior>
    Test coverage for Phase-5 CollaborationService (NO legacy* tests):
    - addProjectCollaborator: validates input, returns success ServiceResult, writes to project_collaborators with correct role
    - getProjectCollaborators: returns ServiceResult with CollaboratorWithUser[], empty array on no rows, error on invalid projectId
    - removeProjectCollaborator: validates input, enforces permission check when options.userId provided, deletes row
    - updateCollaboratorRole: validates role ∈ ['owner','editor','commenter','viewer'], permission-gated, updates row
    - getUserProjectRole: returns 'owner' when projects.owner_id matches userId; returns collaborator role when status='active'; returns null otherwise
    - canUserAccessProject: true when role !== null, false otherwise
    - subscribeToProjectCollaborators: creates channel, invokes callback with fresh data on postgres_changes event, returns unsubscribe fn
    - serviceResultToApiResponse: converts success/error ServiceResult to ApiResponse shape
    Fixtures MUST reflect Phase-5 schema: `projects.owner_id`, invitation rows, viewer/editor role values.
    DELETE every describe/it block referencing `legacy*` methods (lines ~617-670 in current file).
  </behavior>
  <action>
    1. Delete every test block that references `legacyAddProjectCollaborator`, `legacyGetProjectCollaborators`, `legacyRemoveProjectCollaborator`, `legacyUpdateCollaboratorRole`, `legacyGetUserProjectRole`, `legacyCanUserAccessProject` (currently lines ~617-670).
    2. Update remaining Phase-5 method tests so fixtures reflect Phase-5 schema: when testing `getUserProjectRole` owner path, mock `projects` query to return `{ owner_id: userId }` (not old ownership model); when testing collaborators, use role values from `['viewer','editor','commenter','owner']`; status values from Phase-5 schema.
    3. Match the mocking approach used by Phase 5 invitation tests (see read_first grep output). Use the same Supabase client mock pattern, fixture shapes, and helper utilities.
    4. Ensure `CollaborationService.cleanup()` hook in `afterEach` (line ~81) still works.
    5. Run `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts` — all tests must pass.
  </action>
  <verify>
    <automated>grep -n "legacy" src/lib/services/__tests__/CollaborationService.test.ts; test $? -eq 1 && npx vitest run src/lib/services/__tests__/CollaborationService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "legacy" src/lib/services/__tests__/CollaborationService.test.ts` returns nothing.
    - `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts` exits 0 with all tests passing.
    - Test file references `owner_id` (Phase-5 schema) at least once.
  </acceptance_criteria>
  <done>Rewritten test suite green against Phase-5 schema.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up database.test.ts collaboration tests</name>
  <files>src/lib/__tests__/database.test.ts</files>
  <read_first>
    - src/lib/__tests__/database.test.ts lines ~440-490 (collaboration test block)
  </read_first>
  <action>
    For each test in database.test.ts that calls `DatabaseService.addProjectCollaborator` / `DatabaseService.getProjectCollaborators` / other deleted collaboration wrappers:
    - If the corresponding wrapper was DELETED in Plan 02 (Task 2): DELETE the test. It no longer has a target to exercise; the behavior is covered by `CollaborationService.test.ts` (Task 1 above).
    - If the wrapper was KEPT (shouldn't be the case based on Plan 02 expected outcome, but defensively): update the test to call the Phase-5 path.
    Typecheck must pass afterward.
  </action>
  <verify>
    <automated>grep -n "DatabaseService\.\(addProjectCollaborator\|getProjectCollaborators\|removeProjectCollaborator\|updateCollaboratorRole\|getUserProjectRole\|canUserAccessProject\|subscribeToProjectCollaborators\)" src/lib/__tests__/database.test.ts; test $? -eq 1 && npx vitest run src/lib/__tests__/database.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DatabaseService\.\(add\|get\|remove\|update\)Project\(Collaborator\|Collaborators\)" src/lib/__tests__/database.test.ts` returns nothing.
    - `npx vitest run src/lib/__tests__/database.test.ts` exits 0.
  </acceptance_criteria>
  <done>database.test.ts references no deleted collab wrappers; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Final repo-wide verification</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the Definition-of-Done verification commands in sequence and confirm all pass:
    1. `grep -n "legacy" src/lib/services/CollaborationService.ts` → nothing (D-08).
    2. `grep -rn "legacyAdd\|legacyGet\|legacyRemove\|legacyUpdate\|legacyCan" src/ --include='*.ts' --include='*.tsx'` → only matches inside `src/lib/services/ProjectService.ts` and `src/lib/services/__tests__/ProjectService.test.ts` (the unrelated ProjectService.legacy* namespace, explicitly out of scope per CONTEXT.md). NO matches in CollaborationService, database.ts, components, hooks, or CollaborationService test file (D-09).
    3. `npm run type-check` exits 0 (D-10).
    4. `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts src/lib/__tests__/database.test.ts` exits 0 (D-10).
    If any check fails, halt and report which one.
  </action>
  <verify>
    <automated>grep -n "legacy" src/lib/services/CollaborationService.ts; test $? -eq 1 && npm run type-check && npx vitest run src/lib/services/__tests__/CollaborationService.test.ts src/lib/__tests__/database.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four checks above pass.
    - Only residual `legacy*` symbol matches in `src/` are inside ProjectService files (documented out of scope).
  </acceptance_criteria>
  <done>Structural Definition of Done met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of Phase 5 invitation flow</name>
  <what-built>
    Legacy CollaborationService methods deleted, all callers migrated to Phase-5 entry points (new CollaborationService methods + `/api/invitations/create`), tests rewritten against Phase-5 schema.
  </what-built>
  <how-to-verify>
    1. `npm run dev` and open the app.
    2. Sign in as a project owner.
    3. Open a project, navigate to collaborators UI (ProjectCollaborators component / ProjectCollaboration page).
    4. Invite a collaborator by email via the invite modal — confirm: (a) no console errors, (b) POST to `/api/invitations/create` in Network tab returns 200, (c) response contains `inviteUrl`.
    5. Open `inviteUrl` in a second browser/incognito, sign in as the invitee, accept the invite.
    6. Return to owner view — confirm the new collaborator appears in the collaborators list (via `CollaborationService.getProjectCollaborators`).
    7. Change the collaborator's role (e.g. viewer → editor). Confirm the change persists on refresh.
    8. Remove the collaborator. Confirm they disappear from the list.
    9. Confirm no occurrences of "legacy" in the browser console or terminal output.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed.</resume-signal>
  <acceptance_criteria>
    - All 9 verification steps complete successfully with user approval.
    - No console errors referencing collaboration helpers.
  </acceptance_criteria>
  <done>User confirms Phase 5 invitation flow works end-to-end post-migration (D-10).</done>
</task>

</tasks>

<verification>
- `grep -n "legacy" src/lib/services/CollaborationService.ts` → empty (D-08).
- `grep -rn "legacyAdd\|legacyGet\|legacyRemove\|legacyUpdate\|legacyCan" src/` → only ProjectService namespace matches (D-09).
- `npm run type-check` exits 0 (D-10).
- `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts src/lib/__tests__/database.test.ts` exits 0 (D-10).
- Human approval of Phase 5 invitation E2E flow (D-10).
</verification>

<success_criteria>
All Definition of Done criteria (D-08, D-09, D-10) met. Phase 05.1 complete.
</success_criteria>

<output>
Create `.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-03-SUMMARY.md` with: DoD checklist results, test counts before/after, human verification notes.
</output>
