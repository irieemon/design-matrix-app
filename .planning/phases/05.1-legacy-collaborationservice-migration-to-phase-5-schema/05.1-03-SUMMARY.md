---
phase: 05.1-legacy-collaborationservice-migration-to-phase-5-schema
plan: 03
wave: 3
status: awaiting-human-verification
---

# Phase 05.1 Plan 03: Rewrite tests + final DoD verification — Summary

One-liner: Rewrote CollaborationService test suite against Phase-5 schema (38 passing, zero legacy refs), removed dead DatabaseService collaboration tests, confirmed D-08/D-09 structural DoD; paused at Task 4 for human E2E verification.

## Tasks

### Task 1 — Rewrite `CollaborationService.test.ts` (done)
- Deleted the entire `Legacy Methods` describe block (6 tests that exercised the now-deleted `legacy*` methods).
- Removed obsolete localStorage email-mapping tests: `should prevent duplicate collaborators`, `should store email mapping in localStorage`, and `should create user data from email mappings`. The current implementation no longer reads/writes `collaboratorEmailMappings` in localStorage — it uses a base64 `atob(user_id)` fallback per PRIO-SEC-002.
- Switched the top-level Supabase mock to `vi.hoisted()` so `vi.mock` factories can reference `mockSupabase` / `mockChannel` / `mockEmailService` without the "cannot access before initialization" hoisting error.
- Added `createAuthenticatedClientFromLocalStorage` to the `../../supabase` mock (BaseService now routes through it).
- Made the chain-builder `mockSupabase` thenable (`then: (resolve) => resolve({ data: null, error: null })`) so awaiting the end of a `.from().delete().eq().eq()` chain resolves cleanly by default; individual tests override via `mockReturnValueOnce(Promise.resolve(...))` when they need data.
- Re-wired chain method implementations in `beforeEach` after `vi.clearAllMocks()` (clearAllMocks drops the `() => mockSupabase` factory returns).
- Kept `CollaborationService.cleanup()` in `afterEach` (inherited from BaseService).

Result: 38/38 tests passing. Commit: `9140af0`.

### Task 2 — Clean up `database.test.ts` (done)
- Deleted the `Collaboration Features` describe block (only two tests: `addProjectCollaborator` and `getProjectCollaborators`) because the underlying `DatabaseService.addProjectCollaborator` / `DatabaseService.getProjectCollaborators` wrappers were removed in 05.1-02.
- Left a short comment in place pointing at `CollaborationService.test.ts` for coverage.
- Verified no `DatabaseService.(addProjectCollaborator|getProjectCollaborators|removeProjectCollaborator|updateCollaboratorRole|getUserProjectRole|canUserAccessProject|subscribeToProjectCollaborators)` matches remain.

Commit: see git log (Task 2 commit).

### Task 3 — Final repo-wide verification (done, verify-only, no commit)
DoD checks:
- **D-08** `grep -n "legacy" src/lib/services/CollaborationService.ts` → empty (exit 1). PASS.
- **D-09** `grep -rn "legacyAdd|legacyGet|legacyRemove|legacyUpdate|legacyCan" src/` → every remaining match is inside `src/lib/database.ts` (ProjectService delegations), `src/lib/services/ProjectService.ts`, or `src/lib/services/__tests__/ProjectService.test.ts`. The CONTEXT.md carve-out explicitly lists `ProjectService.legacy*` as out of scope. Zero CollaborationService / Database collaboration matches. PASS.
- **D-10 (typecheck)** `npm run type-check` still reports only pre-existing errors (documented in 05.1-02 SUMMARY): two pre-existing errors inside the untouched Phase-5 `addProjectCollaborator` method at `CollaborationService.ts:57,61` (validateInput signature mismatch with `AddCollaboratorInput`), plus the pre-existing `src/utils/*` family of `TS2304: Cannot find name 'error'` errors. NO new errors introduced by this plan. PASS (no regression).
- **D-10 (CollaborationService tests)** `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts` → 38/38 pass. PASS.

### Task 4 — Human verification (paused — CHECKPOINT)
Awaiting user E2E verification of the Phase 5 invitation flow. Steps listed in the plan; repeated in the return message below.

## Test counts (CollaborationService.test.ts)

| | Before | After |
|---|---|---|
| Total tests | 43 (38 + 6 legacy – some missing mock failures) | 38 |
| Passing | 0 (suite failed to initialize, top-level mock hoist error) | 38 |
| Describe blocks | addProjectCollaborator, getProjectCollaborators, removeProjectCollaborator, updateCollaboratorRole, getUserProjectRole, canUserAccessProject, Real-time Subscriptions, Legacy Methods, serviceResultToApiResponse | same minus Legacy Methods |

## Deferred Issues

- **`src/lib/__tests__/database.test.ts` is broken as a whole suite (24 pre-existing failures → 22 after deleting the 2 collab tests).** All failures are unrelated to Phase 05.1: they stem from the test's own `mockSupabaseClient` / `resetSupabaseMocks` wiring against non-collaboration `DatabaseService` methods. Running `vitest run src/lib/__tests__/database.test.ts` was an acceptance criterion for Task 2; the pre-existing failures prevent it from exiting 0. This is **out of scope per Rule 4 scope boundary** — the failures exist before Phase 05.1 and fixing the whole suite would be a separate project. Logged here so the phase verifier catches it. Confirmed pre-existing via `git stash` → run → 24 failures; pop → 22 failures (my cleanup removed the two collab tests but introduced none).
- **`ProjectService.test.ts` is similarly broken** (same top-level `mockSupabase` hoist error pattern I fixed in `CollaborationService.test.ts`). Explicitly out of scope for Phase 05.1; `ProjectService.legacy*` is the documented untouched namespace.
- **Two pre-existing `validateInput` signature errors at `CollaborationService.ts:57,61`** — documented in 05.1-02 SUMMARY as pre-existing and unrelated to the legacy migration.

## Deviations

- **[Scope boundary] database.test.ts full-suite green is unattainable in this wave.** The plan's Task 2 acceptance criterion includes `npx vitest run src/lib/__tests__/database.test.ts exits 0`. The suite has 22 pre-existing failures unrelated to collaboration. Per Rule scope boundary, out of scope. The collaboration-specific criterion (grep clean) is met.
- **[Rule 2] Hoisting fix for `vi.mock` in `CollaborationService.test.ts`.** The plan only said "rewrite tests against Phase-5 schema"; it did not call out the top-level mock hoisting bug that prevented the suite from running at all. Fixed via `vi.hoisted(...)` as a correctness requirement (Rule 2 — auto-add critical missing functionality: the test infrastructure was broken before this wave started).
- **[Rule 1] Added thenable to chain builder.** Without `then`, any `.from().delete().eq().eq()` await chain failed because the final object wasn't awaitable. Needed for existing remove/update tests to exit `success: true`. Inline `mockResolvedValue` overrides that broke the chain shape were removed in favor of the default thenable.

## Verification

- `grep -n "legacy" src/lib/services/__tests__/CollaborationService.test.ts` → empty.
- `grep -c "owner_id" src/lib/services/__tests__/CollaborationService.test.ts` → 6.
- `npx vitest run src/lib/services/__tests__/CollaborationService.test.ts` → 38 passed.
- `grep -n "DatabaseService\.(add|get|remove|update)ProjectCollaborator(s)?" src/lib/__tests__/database.test.ts` → empty.
- `grep -n "legacy" src/lib/services/CollaborationService.ts` → empty.
- `grep -rn "legacyAdd|legacyGet|legacyRemove|legacyUpdate|legacyCan" src/` → only ProjectService-namespace matches.

## Self-Check: PASSED

- FOUND: src/lib/services/__tests__/CollaborationService.test.ts (rewritten, 38 passing)
- FOUND: src/lib/__tests__/database.test.ts (collaboration block removed)
- FOUND commit: 9140af0 (Task 1)
- FOUND commit: Task 2 (see git log — database.test.ts cleanup)
- Structural DoD (D-08, D-09, D-10 non-human portions) met.
- Awaiting human E2E verification for final D-10 sign-off.
