---
phase: 05.1-legacy-collaborationservice-migration-to-phase-5-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProjectCollaborators.tsx
  - src/components/pages/ProjectCollaboration.tsx
  - src/hooks/shared/useDatabase.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "No UI component imports legacy DatabaseService collaboration helpers"
    - "Invite flow in ProjectCollaborators.tsx goes through /api/invitations/create"
    - "Collaborator reads call CollaborationService.getProjectCollaborators directly (not DatabaseService)"
    - "Role updates and removals call CollaborationService directly (not DatabaseService)"
  artifacts:
    - path: "src/components/ProjectCollaborators.tsx"
      provides: "Invite + list + role/remove UI wired to Phase-5 entry points"
    - path: "src/components/pages/ProjectCollaboration.tsx"
      provides: "Collaboration page reading via CollaborationService"
    - path: "src/hooks/shared/useDatabase.ts"
      provides: "Hook surface updated or collaborator helpers removed"
  key_links:
    - from: "src/components/ProjectCollaborators.tsx"
      to: "/api/invitations/create"
      via: "fetch POST with bearer auth"
      pattern: "api/invitations/create"
    - from: "src/components/ProjectCollaborators.tsx"
      to: "CollaborationService.getProjectCollaborators"
      via: "direct service call"
      pattern: "CollaborationService\\.getProjectCollaborators"
---

<objective>
Migrate all UI call sites off `DatabaseService` collaboration helpers onto Phase-5 entry points. After this plan, no component or hook imports `DatabaseService.addProjectCollaborator / getProjectCollaborators / removeProjectCollaborator / updateCollaboratorRole / getUserProjectRole / canUserAccessProject / subscribeToProjectCollaborators`.

Purpose: Unblock deletion of legacy methods in Wave 2 by removing upstream callers first (safest order).
Output: UI components and hooks wired directly to CollaborationService methods and `/api/invitations/create`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-CONTEXT.md
@.planning/phases/05-real-time-collaboration/05-02-invitations-backend-PLAN.md
@src/lib/services/CollaborationService.ts
@src/components/ProjectCollaborators.tsx
@src/components/pages/ProjectCollaboration.tsx
@src/hooks/shared/useDatabase.ts

<interfaces>
CollaborationService (src/lib/services/CollaborationService.ts) Phase-5 static methods available:
- addProjectCollaborator(input: AddCollaboratorInput): Promise<ServiceResult<boolean>>
- getProjectCollaborators(projectId: string, options?): Promise<ServiceResult<CollaboratorWithUser[]>>
- removeProjectCollaborator(projectId, userId, options?): Promise<ServiceResult<boolean>>
- updateCollaboratorRole(projectId, userId, newRole, options?): Promise<ServiceResult<boolean>>
- getUserProjectRole(projectId, userId, options?): Promise<ServiceResult<ProjectRole | null>>
- canUserAccessProject(projectId, userId, options?): Promise<ServiceResult<boolean>>
- subscribeToProjectCollaborators(projectId, callback, options?): () => void

ServiceResult<T> = { success: true; data: T } | { success: false; error: {...} }

/api/invitations/create — POST { projectId, inviteeEmail, role } with Bearer token; returns { inviteUrl }.
See src/components/collaboration/InviteCollaboratorModal.tsx (commit c97efb5) for reference auth pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProjectCollaborators.tsx to Phase-5 entry points</name>
  <files>src/components/ProjectCollaborators.tsx</files>
  <read_first>
    - src/components/ProjectCollaborators.tsx (full file)
    - src/components/collaboration/InviteCollaboratorModal.tsx (for /api/invitations/create fetch pattern with bearer auth)
    - src/lib/services/CollaborationService.ts (Phase-5 methods lines 50-486)
  </read_first>
  <action>
    Replace every `DatabaseService.*` collaboration call in this file:
    1. Line ~64 `DatabaseService.getProjectCollaborators(projectId)` → `CollaborationService.getProjectCollaborators(projectId)`; unwrap `ServiceResult` (`result.success ? result.data : []`).
    2. Line ~78 `DatabaseService.subscribeToProjectCollaborators(projectId, setCollaborators)` → `CollaborationService.subscribeToProjectCollaborators(projectId, setCollaborators)`.
    3. Line ~93 `DatabaseService.addProjectCollaborator(...)` → POST to `/api/invitations/create` with `Authorization: Bearer <accessToken>` header, body `{ projectId, inviteeEmail: userEmail, role }`. Use the same auth pattern as InviteCollaboratorModal.tsx. Treat HTTP 2xx as success.
    4. Line ~120 `DatabaseService.updateCollaboratorRole(projectId, userId, newRole)` → `CollaborationService.updateCollaboratorRole(projectId, userId, newRole, { userId: currentUser.id })`; unwrap ServiceResult.
    5. Line ~143 `DatabaseService.removeProjectCollaborator(projectId, collaborator.user_id)` → `CollaborationService.removeProjectCollaborator(projectId, collaborator.user_id, { userId: currentUser.id })`; unwrap ServiceResult.
    Remove the `DatabaseService` import if no longer used. Add `import { CollaborationService } from '@/lib/services/CollaborationService'`.
    Do NOT reintroduce the legacy `onInvite` callback (removed in 8e67ca5).
  </action>
  <verify>
    <automated>grep -n "DatabaseService\.\(add\|get\|remove\|update\|subscribeToProject\)" src/components/ProjectCollaborators.tsx; test $? -eq 1 && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DatabaseService" src/components/ProjectCollaborators.tsx` returns no collaboration-helper matches (only non-collab refs allowed, or no refs at all).
    - `grep -n "/api/invitations/create" src/components/ProjectCollaborators.tsx` returns exactly 1 match.
    - `grep -n "CollaborationService" src/components/ProjectCollaborators.tsx` returns ≥1 match.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>File compiles, uses CollaborationService for reads/role/remove and `/api/invitations/create` for invites.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ProjectCollaboration.tsx page to CollaborationService</name>
  <files>src/components/pages/ProjectCollaboration.tsx</files>
  <read_first>
    - src/components/pages/ProjectCollaboration.tsx (full file)
    - src/lib/services/CollaborationService.ts (getUserProjectRole, getProjectCollaborators signatures)
  </read_first>
  <action>
    Replace:
    1. Line ~31 `DatabaseService.getUserProjectRole(currentProject.id, currentUser.id)` → `CollaborationService.getUserProjectRole(currentProject.id, currentUser.id, { userId: currentUser.id })`; unwrap `ServiceResult` → extract `result.data` (ProjectRole | null).
    2. Line ~42 `DatabaseService.getProjectCollaborators(currentProject.id)` → `CollaborationService.getProjectCollaborators(currentProject.id, { userId: currentUser.id })`; unwrap to array (empty array on failure).
    Add `import { CollaborationService } from '@/lib/services/CollaborationService'`. Remove `DatabaseService` import if now unused.
  </action>
  <verify>
    <automated>grep -n "DatabaseService\.\(getUserProjectRole\|getProjectCollaborators\)" src/components/pages/ProjectCollaboration.tsx; test $? -eq 1 && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DatabaseService\.getUserProjectRole\|DatabaseService\.getProjectCollaborators" src/components/pages/ProjectCollaboration.tsx` returns nothing.
    - `grep -n "CollaborationService\.\(getUserProjectRole\|getProjectCollaborators\)" src/components/pages/ProjectCollaboration.tsx` returns 2 matches.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Page loads role and collaborator list via CollaborationService directly.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate useDatabase hook — remove or rewire collaborator helpers</name>
  <files>src/hooks/shared/useDatabase.ts</files>
  <read_first>
    - src/hooks/shared/useDatabase.ts (full file, especially lines 200-280)
    - Grep for each exported helper to see if it has remaining callers: `getProjectCollaborators`, `addProjectCollaborator`, `removeProjectCollaborator` from useDatabase.
  </read_first>
  <action>
    For each hook helper in useDatabase.ts that currently calls `DatabaseService.{getProjectCollaborators|addProjectCollaborator|removeProjectCollaborator}` (lines ~223, ~248, ~266):
    1. Run `grep -rn "useDatabase.*\(getProjectCollaborators\|addProjectCollaborator\|removeProjectCollaborator\)" src/` to find consumers.
    2. If a helper has ZERO consumers after Task 1 removed ProjectCollaborators.tsx usage, DELETE the helper entirely (per D-03: no dead code).
    3. If a helper still has consumers, rewire it: reads → `CollaborationService.getProjectCollaborators` (unwrap); invites → `/api/invitations/create` fetch; removals → `CollaborationService.removeProjectCollaborator` (unwrap). Keep the hook's public return shape stable.
    Remove unused `DatabaseService` import at the top of the file if no other usage remains.
  </action>
  <verify>
    <automated>grep -n "DatabaseService\.\(add\|get\|remove\|update\)Project\(Collaborator\|Collaborators\)\|DatabaseService\.subscribeToProjectCollaborators" src/hooks/shared/useDatabase.ts; test $? -eq 1 && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DatabaseService\.\(add\|get\|remove\|update\)Project\(Collaborator\|Collaborators\)" src/hooks/shared/useDatabase.ts` returns nothing.
    - `npm run type-check` exits 0.
    - No TypeScript errors from downstream consumers of useDatabase.
  </acceptance_criteria>
  <done>Hook file no longer depends on legacy DatabaseService collaboration surface; typecheck green.</done>
</task>

</tasks>

<verification>
After all tasks:
- `grep -rn "DatabaseService\.\(add\|get\|remove\|update\)Project\(Collaborator\|Collaborators\)\|DatabaseService\.subscribeToProjectCollaborators\|DatabaseService\.\(getUserProjectRole\|canUserAccessProject\)" src/ --include='*.ts' --include='*.tsx'` returns only matches inside `src/lib/database.ts` (delegations) or `src/lib/__tests__/database.test.ts` (handled in Wave 2/3). No UI/hook matches remain.
- `npm run type-check` exits 0.
</verification>

<success_criteria>
All UI components and hooks use Phase-5 entry points. Remaining legacy references live only in `src/lib/database.ts`, `src/lib/services/CollaborationService.ts` (the `legacy*` methods themselves), and test files — all handled by later waves.
</success_criteria>

<output>
Create `.planning/phases/05.1-legacy-collaborationservice-migration-to-phase-5-schema/05.1-01-SUMMARY.md` with list of files migrated and the Phase-5 entry point chosen per call site.
</output>
