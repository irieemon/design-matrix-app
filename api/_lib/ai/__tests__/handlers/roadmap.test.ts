import { describe, it } from 'vitest';

/**
 * Wave 0 test stubs for the roadmap handler migration (Plan 03).
 *
 * These tests will be implemented when the roadmap handler is migrated
 * from api/ai.ts to api/_lib/ai/handlers/generateRoadmap.ts.
 */
describe('generateRoadmap handler', () => {
  it.todo('returns same JSON shape as legacy handler');
  it.todo('handles generate-roadmap-v2 action');
});
