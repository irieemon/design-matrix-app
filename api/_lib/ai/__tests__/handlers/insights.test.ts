import { describe, it } from 'vitest';

/**
 * Wave 0 test stubs for the insights handler migration (Plan 02).
 *
 * These tests will be implemented when the insights handler is migrated
 * from api/ai.ts to api/_lib/ai/handlers/generateInsights.ts.
 */
describe('generateInsights handler', () => {
  it.todo('returns same JSON shape as legacy handler');
  it.todo('handles Anthropic fallback');
});
